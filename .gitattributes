# Auto detect text files and perform LF normalization
* text=auto

# Explicitly declare text files that must be normalized and checked out as LF.
*.rs text eol=lf
*.toml text eol=lf
*.json text eol=lf
*.md text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
*.txt text eol=lf

# TypeScript/JavaScript files
*.ts text eol=lf
*.tsx text eol=lf
*.js text eol=lf
*.mjs text eol=lf
*.jsx text eol=lf

# HTML/CSS files
*.html text eol=lf
*.css text eol=lf
*.scss text eol=lf

# Shell scripts
*.sh text eol=lf
*.ps1 text eol=lf

# NSIS sources and generated includes are byte-bound release inputs.
*.nsi text eol=lf
*.nsh text eol=lf

# Denote all files that are truly binary and should not be modified.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
*.ttf binary
*.exe binary
*.dll binary

# Desktop visual baselines are binary review artifacts. Keep the future
# platform/scale/locale baselines in Git LFS instead of inflating normal Git
# history; no test command is allowed to update these files implicitly.
tests/e2e/visual-baselines/**/*.png filter=lfs diff=lfs merge=lfs -text

# Trellis: append-only developer journals should merge cleanly across
# parallel sessions/worktrees — each session only appends a new block, so
# there is nothing to actually conflict on.
#
# Do NOT add a rule for workspace/*/index.md here — it is fully regenerated
# every session, so a real conflict there is expected and safe to resolve by
# picking either side (task state lives in task.json, not index.md). See
# .trellis/spec/cli/backend/directory-structure.md for details.
.trellis/workspace/*/journal-*.md merge=union
