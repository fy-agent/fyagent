# Secret-surface inventory

Status: 2026-08-15 V9 final-review-aligned static source inventory on base `afc317a7...`; no runtime scan. Product scope is `contract_schema + codex_feature_runtime`; repository-global is not claimed. The exact registered path/owner/evidence authority is `research/codex-secret-call-graph.md`. Coverage is mechanical: source freeze must regenerate the sorted path inventory and fail on every unregistered addition, omission or move. `SNV7-001..006` and the V9 closure below are design requirements, not implemented/PASS claims.

## Codex first-slice surfaces that #35 must close

| Surface | Current value path | Required replacement |
| --- | --- | --- |
| Provider DB | `providers.settings_config` → `/auth/OPENAI_API_KEY` or config TOML `experimental_bearer_token` | token-free Provider row; device-local owner binding/ref is joined only at read time; scrub after an approved candidate activation |
| Provider IPC | `get_providers` returns serializable `Provider` | closed token-free `CodexProviderPublicDto` + separate `SecretOwnerCredentialSummary`; no redact-after-serialization path |
| Provider mutation IPC | add/update/import/deeplink accepts full Provider/`apiKey` | closed token-free `CodexProviderMutationDto`; reject inline/unknown secret-shaped fields before any write; native capture stages/ref-binds through the frozen flow |
| Renderer presets/forms/TOML helpers | `codexProviderPresets.ts` creates `auth.OPENAI_API_KEY`; `useCodexConfigState.ts` stores, reveals and rewrites the value; `providerConfigUtils.ts` extracts/replaces active/top-level bearer tokens | presets carry non-secret structure only; Codex form state becomes owner/readiness/native-capture state; no renderer token parser/updater is an authority or scrubber |
| Provider feature drafts/forms/cards | Add/Edit dialogs, `useCodexProviderFeatures`, Codex form/editor sections and `ProviderCard` pass or inspect full `Provider.settingsConfig`; `codexTemplates.ts` emits `OPENAI_API_KEY: ""` | closed token-free feature-draft/public/mutation DTOs only; feature analysis/patch never receives a full Provider or secret-shaped field; card/form state consumes summary/readiness; delete the empty-key substitute instead of treating it as redaction |
| Codex live sync/backfill | `services/config.rs` writes Provider auth/config live, extracts live bearer material and restores it into stored Provider auth | no direct Codex sync/backfill; immutable #55 plan → #41 writer for an exact sink; non-secret backfill only |
| startup publication | DB backup/default import/workers may run before secret authority and reconciliation are clean | exact `open store → no-backup DB preflight → same AppState/SecretService → reconcile → app.manage/static registration → Clean sanitized backup → publish gate → workers`; Blocked creates no backup and starts/publishes no worker or consumer |
| usage/balance | `Provider::resolve_usage_credentials` creates key `String`; `usage.ts`, `get_balance` and `testUsageScript` accept or forward values | owner/ref request + fixed native `usageProbe` resolver; no value IPC, renderer key state or primary-secret QuickJS path |
| coding-plan fixed adapters | `UsageScriptModal.tsx` selects `providerCredentials.apiKey` → `subscription.ts` → `commands/coding_plan.rs` → `services/coding_plan.rs` fixed Kimi/Zhipu/MiniMax HTTP adapters | Provider owner/ref no-value request; fixed adapter resolves with `consumer=FixedRuntimeConsumer::CodingPlanUsageProbe` and closed `CodingPlanPrimaryAdapter` at the final header/send boundary, under `usageProbe/codex_feature_runtime` |
| model fetch | `CodexFormFields` forwards `codexApiKey` through `fetchModelsForConfig`; native command accepts `api_key: String` | Provider owner/ref request + controlled `modelFetch` resolver at final send; no renderer/API value field |
| proxy/forwarder | Codex adapter copies key into owned `AuthInfo(String)` and failover loop | non-sensitive auth requirement; per-attempt resolve at final send; secret failure stops failover |
| takeover/live backup | raw Codex `{auth,config}` persisted/restored | structural placeholder; fresh capability for restore; OAuth preserved in place |
| terminal | config values copied to child env/temp script | Codex primary-secret path rejected, effect none |
| failover/deeplink | full Provider returned; native request/event reaches renderer where `deepLinkConfigPreview.ts`/`DeepLinkImportDialog.tsx` decode, mask and preview secret-bearing query or remote config | sanitized candidate DTO; native ingress rejects raw, percent-encoded and remote-config Codex secret fields before parse/merge/event; renderer never decodes, masks or previews them; generated-canary failures never echo input |
| import/restore/sync | `sync_protocol.rs` callers can mutate Skills/main DB before complete staged secret adjudication, then attempt direct post-sync write | register one temp live object without staged source validation; mint its temp authority/token plus material-free projection; obtain #55 admission and an authority-match receipt; run #35 prepare/confirm; then construct the exact `ImportCutoverCoordinatorContext` as the sole authority before any staged source validation/scrub/readback or cutover. Only after validation/scrub/readback may cutover and live owner/binding finalize run. Resume input is only `{stageId,expectedResumeCas:{revision,digest}}` and returns `currentResumeCas` of the same shape; stale nonce/admission/CAS is zero-write |
| Provider delete | `App.tsx` opens a generic irreversible dialog then `providers.ts`/`mutations.ts` call delete directly | bound/unbound preview is no-value; legacy is blocked and receives no impact id. Stale Provider impact is Provider-owned `PROVIDER_DELETE_IMPACT_STALE + refreshProviderDeleteImpact + effect=none`, outside `SecretErrorCode`/`SecretUserAction`; `refreshDeleteImpact` remains secret-delete-only |
| UniversalProvider | public `apiKey` and `to_codex_provider()` copy into Provider | cannot remain on Codex path; migration/redacted projection or explicitly disabled Codex conversion until ref-backed |
| startup history backup/export/diagnostic | `codex_history_migration.rs::backup_provider_settings_config` copies raw `settingsConfig`; serialization/DB backups can copy settings | before the startup no-value gate, no history/settings backup exists; after a Clean gate, new generations contain structural placeholders/non-secret config only; existing generations are scan/report-only |
| logs/errors | generic provider/platform errors may interpolate inputs | stable secret error only; canary scan |
| source/fixture expectations | existing Rust/TS command, form, feature, template, preset, deep-link and migration tests assert plaintext/empty-key Provider/live/backfill/TOML behavior or check in secret-shaped literals | rewrite as runtime-generated-canary, token-free and structural negative assertions for owner/ref DTOs, discovery, ingress rejection, scrub and no-value writers; fixtures are part of Level 1/2, never blanket allowlisted |
| `SNV7-001` Codex `OPENAI_*` environment chain | native scanner/manager, commands, TS API/types and banner currently carry value and absolute source/backup path; delete/restore can create plaintext backup | Codex returns presence/name/stable source category only. Process environment, Windows HKCU, Windows HKLM and enumerated shell-file category are **adjacent-blocked coverage observations**, never current-scrubbable `LegacySourceRef`; no value/path reaches receipt/IPC/UI, and Codex delete/restore creates no plaintext backup. Generated canary must be absent from every result, event, UI, log and backup |
| `SNV7-002` Codex common config | legacy config JSON/migration/SQLite `common_config_codex`, raw config commands/API, localStorage migration, modal/editor and live merge can round-trip a secret-bearing TOML snippet | reject new Codex secret-bearing snippet before any write. Existing JSON, backup, migrated, SQLite, localStorage and live-merge hits become separate no-value adjacent-blocked observations with stable category/revision; they never become current-scrubbable `LegacySourceRef`, and raw snippet/path never returns through coverage receipt, IPC or localStorage. Public DTO is closed and non-secret |
| `SNV7-003` public Provider downstream | public `Provider.settingsConfig` flows through schema/query/list/sort/forms/shared API-key UI/MSW fixtures | split Codex internal/public/mutation types; no public Codex object contains `settingsConfig`; shared `ApiKeySection`/`ApiKeyInput` is unreachable from Codex. List/update/sort fixtures use closed token-free Codex DTOs |
| `SNV7-004` request overrides | Codex form can persist arbitrary local proxy header/body overrides; native proxy copies header/body through shared maps/`Vec` | reject new Codex secret/header/body arbitrary override mutations before write and fail closed on existing Codex rows. Primary material uses only an owner-private, single-send, zeroizing transport; it never enters a shared raw `HeaderMap`/`Vec`. Retained non-Codex override support remains Level 3 debt |
| `SNV7-005` stream/proxy diagnostics | stream check, proxy/failover errors and health rows can perform network work or preserve raw URL/error/body/message | Codex has no active secret-bearing stream check. Map to closed status/category/latency before DB/UI; raw URL, upstream error/body/message are forbidden. Generated reflection canary has zero matches in response, DB, toast, log and error body |
| `SNV7-006` Codex MCP `env`/`http_headers` | unified MCP JSON, SQLite, commands/UI, Codex TOML import/sync and export/sync artifacts carry arbitrary credential values | classify as `codexMcpEnvOrHeaderCredential`, Level 3 adjacent debt. Preserve exact inventory/no-regression, replace static secret fixtures with generated canaries, and fail any new/moved occurrence. This debt is not Provider-primary Level 2 PASS scope and cannot support a repository-global claim |

## Exact base path register added by this audit

All paths below are existing base-tree surfaces and have the sole implementation owner `main integration`. Focused evidence is frozen in call graph §9.4.

| Slice | Exact paths |
| --- | --- |
| native live value/backfill and fixtures | `src-tauri/src/services/config.rs`; `src-tauri/tests/provider_service.rs`; `src-tauri/tests/import_export_sync.rs` |
| renderer value factory/state/TOML helper and fixture | `src/config/codexProviderPresets.ts`; `src/components/providers/forms/hooks/useCodexConfigState.ts`; `src/utils/providerConfigUtils.ts`; `tests/utils/providerConfigUtils.codex.test.ts` |
| Provider feature draft/public/mutation UI and API | `src/components/providers/AddProviderDialog.tsx`; `src/components/providers/EditProviderDialog.tsx`; `src/hooks/useCodexProviderFeatures.ts`; `src/components/providers/forms/CodexFormFields.tsx`; `src/components/providers/forms/CodexConfigSections.tsx`; `src/components/providers/forms/CodexConfigEditor.tsx`; `src/components/providers/ProviderCard.tsx`; `src/lib/api/usage.ts`; `src/config/codexTemplates.ts` |
| coding-plan primary-key chain | `src/components/UsageScriptModal.tsx`; `src/lib/api/subscription.ts`; `src-tauri/src/commands/coding_plan.rs`; `src-tauri/src/services/coding_plan.rs` |
| deep-link native-reject/render boundary | `src/utils/deepLinkConfigPreview.ts`; `src/components/DeepLinkImportDialog.tsx`; `tests/utils/deepLinkConfigPreview.test.ts`; `tests/components/DeepLinkImportDialog.test.tsx` |
| staged sync protocol and callers | `src-tauri/src/services/sync_protocol.rs`; `src-tauri/src/services/webdav_sync.rs`; `src-tauri/src/services/webdav_sync/archive.rs`; `src-tauri/src/services/s3_sync.rs` |
| startup history migration/backup | `src-tauri/src/codex_history_migration.rs` (implementation + inline tests); `src-tauri/src/lib.rs` (startup call + inline tests) |
| Provider delete preview/confirm | `src/App.tsx`; `src/hooks/useProviderActions.ts`; `src/lib/api/providers.ts`; `src/lib/query/mutations.ts`; `src/i18n/locales/en.json`; `src/i18n/locales/ja.json`; `src/i18n/locales/zh.json`; `src/i18n/locales/zh-TW.json`; `tests/integration/App.test.tsx`; `tests/hooks/useProviderActions.test.tsx`; `tests/lib/providersApi.codexFeatures.test.ts`; `tests/msw/handlers.ts` |
| checked fixture expansion | `src-tauri/tests/provider_commands.rs`; `src-tauri/tests/mcp_commands.rs`; `tests/hooks/useCodexProviderFeatures.test.tsx`; `tests/hooks/useCodexConfigState.catalog.test.ts`; `tests/hooks/useAddProviderMutation.test.tsx`; `tests/components/AddProviderDialog.test.tsx`; `tests/components/EditProviderDialog.test.tsx`; `tests/components/CodexFormFields.capabilities.test.tsx`; `tests/config/codexTemplates.test.ts`; `tests/config/therouterProviderPresets.test.ts`; `tests/config/subrouterProviderPresets.test.ts`; `tests/config/xaiOauthProviderPresets.test.ts` |
| `SNV7-001` Codex env (16 path/category entries) | `src-tauri/src/services/env_checker.rs`; `src-tauri/src/commands/env.rs`; `src-tauri/src/services/env_manager.rs`; `src-tauri/src/services/mod.rs`; `src-tauri/src/commands/mod.rs`; `src-tauri/src/lib.rs`; `src/lib/api/env.ts`; `src/types/env.ts`; `src/components/env/EnvWarningBanner.tsx`; `src/App.tsx`; `tests/codexWindowsUserScopeContract.test.ts`; `tests/integration/App.test.tsx`; `src/i18n/locales/en.json`; `src/i18n/locales/ja.json`; `src/i18n/locales/zh.json`; `src/i18n/locales/zh-TW.json` |
| `SNV7-002` common config (21 path/category entries) | `src-tauri/src/app_config.rs`; `src-tauri/src/database/migration.rs`; `src-tauri/src/database/dao/settings.rs`; `src-tauri/src/commands/config.rs`; `src-tauri/src/commands/provider.rs`; `src-tauri/src/services/provider/mod.rs`; `src-tauri/src/services/provider/live.rs`; `src-tauri/src/services/proxy.rs`; `src-tauri/src/lib.rs`; `src/lib/api/config.ts`; `src/components/providers/forms/hooks/useCodexCommonConfig.ts`; `src/components/providers/forms/CodexCommonConfigModal.tsx`; `src/components/providers/forms/CodexConfigEditor.tsx`; `src/components/providers/forms/ProviderForm.tsx`; `src-tauri/tests/app_config_load.rs`; `src-tauri/tests/provider_service.rs`; `src-tauri/src/database/tests.rs`; `src-tauri/tests/import_export_sync.rs`; `tests/hooks/useCommonConfigSave.test.tsx`; `tests/components/CommonConfigModalBehavior.test.tsx`; `tests/components/CommonConfigEditor.test.tsx` |
| `SNV7-003` public Provider downstream (20 path/category entries) | `src/types.ts`; `src/lib/schemas/provider.ts`; `src/lib/query/queries.ts`; `src/lib/query/mutations.ts`; `src/lib/api/providers.ts`; `src/components/providers/ProviderList.tsx`; `src/hooks/useDragSort.ts`; `src/components/providers/forms/CodexFormFields.tsx`; `src/components/providers/forms/shared/ApiKeySection.tsx`; `src/components/providers/forms/ApiKeyInput.tsx`; `tests/msw/state.ts`; `tests/msw/handlers.ts`; `tests/components/ProviderList.test.tsx`; `tests/hooks/useDragSort.test.tsx`; `tests/hooks/useUpdateProviderMutation.test.tsx`; `tests/hooks/useAddProviderMutation.test.tsx`; `tests/components/CodexFormFields.capabilities.test.tsx`; `tests/components/AddProviderDialog.test.tsx`; `tests/components/EditProviderDialog.test.tsx`; `tests/lib/providersApi.codexFeatures.test.ts` |
| `SNV7-004` request overrides (13 path/category entries) | `src/lib/requestOverrides.ts`; `src/components/providers/forms/ProviderForm.tsx`; `src/components/providers/forms/LocalProxyRequestOverridesField.tsx`; `src/components/providers/forms/CodexFormFields.tsx`; `src/components/providers/forms/ClaudeFormFields.tsx`; `src/components/providers/forms/GrokBuildProviderForm.tsx`; `src-tauri/src/provider.rs`; `src-tauri/src/proxy/forwarder.rs`; `src-tauri/src/proxy/hyper_client.rs`; `src-tauri/src/proxy/providers/codex.rs`; `src-tauri/src/services/proxy.rs`; `tests/lib/requestOverrides.test.ts`; `src-tauri/tests/provider_service.rs` |
| `SNV7-005` stream/proxy diagnostics (22 path/category entries) | `src-tauri/src/commands/stream_check.rs`; `src-tauri/src/services/stream_check.rs`; `src-tauri/src/database/dao/stream_check.rs`; `src-tauri/src/database/schema.rs`; `src/lib/api/connectivity-check.ts`; `src/hooks/useStreamCheck.ts`; `src/components/providers/ProviderList.tsx`; `tests/components/ProviderList.test.tsx`; `src-tauri/src/proxy/error.rs`; `src-tauri/src/proxy/types.rs`; `src-tauri/src/commands/proxy.rs`; `src-tauri/src/services/proxy.rs`; `src-tauri/src/database/dao/proxy.rs`; `src/types/proxy.ts`; `src/lib/api/proxy.ts`; `src/lib/query/proxy.ts`; `src/components/proxy/ProxyPanel.tsx`; `tests/hooks/useProxyStatus.test.tsx`; `src-tauri/src/database/dao/failover.rs`; `src-tauri/src/commands/failover.rs`; `src/lib/api/failover.ts`; `src/lib/query/failover.ts` |
| `SNV7-006` MCP env/header debt (35 path/category entries) | `src-tauri/src/app_config.rs`; `src-tauri/src/services/mcp.rs`; `src-tauri/src/database/dao/mcp.rs`; `src-tauri/src/database/schema.rs`; `src-tauri/src/database/migration.rs`; `src-tauri/src/commands/mcp.rs`; `src-tauri/src/mcp/mod.rs`; `src-tauri/src/mcp/codex.rs`; `src-tauri/src/codex_config.rs`; `src-tauri/src/lib.rs`; `src/types.ts`; `src/lib/api/mcp.ts`; `src/hooks/useMcp.ts`; `src/components/mcp/McpWizardModal.tsx`; `src/components/mcp/UnifiedMcpPanel.tsx`; `src/components/mcp/McpFormModal.tsx`; `src/components/mcp/useMcpValidation.ts`; `src-tauri/src/database/backup.rs`; `src-tauri/src/commands/import_export.rs`; `src-tauri/src/services/sync_protocol.rs`; `src-tauri/src/services/webdav.rs`; `src-tauri/src/services/s3.rs`; `src-tauri/src/services/webdav_sync.rs`; `src-tauri/src/services/webdav_sync/archive.rs`; `src-tauri/src/services/s3_sync.rs`; `src-tauri/src/commands/webdav_sync.rs`; `src-tauri/src/commands/s3_sync.rs`; `src-tauri/tests/mcp_commands.rs`; `src-tauri/tests/import_export_sync.rs`; `tests/components/UnifiedMcpPanel.test.tsx`; `tests/components/McpFormModal.test.tsx`; `tests/hooks/useMcpBulkToggle.test.tsx`; `tests/hooks/useMcpValidation.test.tsx`; `tests/msw/state.ts`; `tests/msw/handlers.ts` |

The V7 supplemental floor is therefore **127 exact path/category entries, 111 unique exact paths, across six categories**. Repeated files across categories describe distinct observed behaviors but never create a second writer: the sole canonical owner remains `main integration`. §9.4 is the owner/evidence authority.

## Frozen cross-flow ordering for the next candidate

- Staged import is exact: `temp authority/token + material-free projection → #55 admission → authority-match receipt → #35 prepare/confirm → construct exact ImportCutoverCoordinatorContext → staged source validation/scrub/readback → cutover → live owner/binding finalize`. The context is the sole authority for every source-validation, scrub/readback and cutover step; it never appears after those steps. The public resume request is only `{stageId,expectedResumeCas:{revision,digest}}`; every result returns `currentResumeCas:{revision,digest}`. Full staged identity is an internal preimage. Old nonce, admission or CAS is stale and performs zero writes.
- Typed capture uses the existing 15-command surface. `list_secret_backend_options` reads the native owner, legacy-source and binding snapshot and mints one single-use `SecretCaptureIntentId`; `begin_secret_capture` accepts only that intent id plus the selected backend. `retryCapture`, `captureReplacement`, `chooseBackend` and `resolveLegacyConflict` all resolve to this same list→begin flow. Terminal expiry first refreshes the summary/owner card, then mints fresh capture or rotation authority; it never reuses the old candidate or intent.
- Every backend delete and every fresh missing readback in capture compensation, explicit discard/delete and activation cleanup has its own authorization and durable checkpoint. A delete authorization cannot authorize the readback. Persistent `revoked` state requires an explicit `Revoke` authorization; probe observations never persist it. Every record/read/delete scope binds the device-store instance and the exact registered backend object.
- The command/journal/recovery totals remain **15 #35 commands, 8 journals and 4 recovery kinds**. #55 owns plan/admission, #41 owns apply/Provider-lease coordination, and `main integration` owns shared existing files. No v17, fallback, implementation/runtime PASS or repository-global claim is introduced here.

## V9 closed-surface alignment

- Initial staged activation and post-crash resume are different closed types. `resume_staged_import_cutover` accepts only `{stageId,expectedResumeCas:{revision,digest}}`. Every resume result data arm contains exactly `stageId|currentResumeCas|status|action|issue`: `activated|alreadyActivated` returns `issue=null`, while `recoveryRequired` returns its typed issue. Result data cannot contain `schemaVersion`, `auditEventId`, candidate, owner, secret ref, summary, initial-activation payload or any other field. The common command envelope owns version/command id, and audit is emitted independently.
- `LegacySourceCoverageReceipt` is an opaque `pub(crate)`, non-Clone/non-Serde/non-Debug move-only type so store, Provider and #35 sibling modules can name/move/consume it; its fields remain private. Its `pub(crate)` `checked_from_complete_inventory_authority` factory consumes `CompleteLegacySourceInventoryAuthority`, which only the main-integration `CodexLegacySourceInventoryBridge` can privately construct, so siblings cannot forge a receipt. One receipt atomically binds non-value-derived `LegacySourceInventoryRevision`, fixed complete `CompleteLegacySourceCoverageIdentity`, exact `CurrentLegacySourceExpectations`, and category/state-only `AdjacentBlockedLegacySourceObservation` rows; proof and data may not be split. The identity has exactly 11 domains: `currentProviderLiveScrubbable` and `processEnvironment|windowsRegistryCurrentUser|windowsRegistryLocalMachine|shellStartupFile|commonConfigJson|commonConfigBackup|commonConfigMigrated|commonConfigSqlite|rendererLocalStorage|liveConfigMerge`, each with structural revision/presence/count. Current refs may contain only an opaque non-value-derived `LegacySourceLocationId`; the receipt forbids raw path, raw locator, source value and value-derived digest. Supplemental observations never become refs. Public `LegacySourceCoverageView` remains no-authority. Startup reconcile, every owner summary/readiness projection, capture options and begin/revalidation, and Provider-delete preview and confirm each obtain/revalidate a fresh atomic receipt through the bridge. Even an empty inventory blocks unless all 11 domains and the current inventory revision prove complete absence.
- Build composition is ordered: #35 core traits/APIs compile independently and pass focused core tests first; #55, #41 and main-adapter types then land in their own owner files; only the single `main integration` adapter/composition owner wires those published types and runs the full Rust gate. `src-tauri/src/secret/backend.rs` cannot reference a concrete external type that does not yet exist.
- Registration is exactly **15 #35 secret commands plus one separate main-integration `resume_staged_import_cutover` handler**. The resume handler is not a sixteenth `SecretCommandName`. `src-tauri/src/lib.rs` must statically assert both sets and the staged phase-crash UAT must exercise the separate handler.
- Durable `DeviceInstanceId` identifies the persisted device namespace; process-local `DeviceSecretStoreInstanceId` identifies one lifetime-locked store open. They are never interchangeable or serialized together as one ID. `#35 module` owns the backend registry and private operation broker; adapters/main integration may resolve registered handles but cannot mint registry or broker authority.
- The backend policy has exactly five operations: `CaptureVerify|Validate|ResolveForApply|Delete|Revoke`. Every fresh missing readback maps to `Validate`, never a sixth `Missing` operation and never ordinary probe; its independent authorization remains downstream of the durable delete checkpoint.
- Exact verification requires locked `--all-targets` Rust **1.85.0** checks on native macOS and native Windows evidence hosts. The repository's current Rust 1.97.1 run is useful but cannot substitute for either 1.85.0 result.

## Existing adjacent value-bearing surfaces

Static grep also identifies secret values outside the Codex Provider-primary MVP:

- WebDAV password and S3 secret access key in settings/IPC;
- coding-plan **independent** credentials only: ZenMux's separately hand-entered key/base URL, Volcengine account AK/SK, Zhipu team identifiers/login tokens and other team/login material;
- usage-script `accessToken` and arbitrary request fields; generic script `apiKey` remains rejected for Codex primary material;
- non-Codex Provider settings for Claude/Gemini/OpenCode/Hermes/OpenClaw;
- managed OAuth token managers;
- legacy UniversalProvider shared API key and non-Codex conversion;
- Codex MCP `env`/`http_headers` values classified as `codexMcpEnvOrHeaderCredential` and retained only as explicit Level 3 inventory debt;
- historical credential-shaped fixtures outside the Codex registered path set.

The Provider-primary coding-plan key is **not** adjacent debt. Fixed adapters that derive it from the current Codex Provider belong to `usageProbe` and `codex_feature_runtime`; only the independent credentials named above stay in inventory debt.

These paths mean the existing repository is **not** globally free of secret-bearing UI/IPC/storage. A scanner limited to #35/#55/#41 DTOs and the registered Codex runtime artifact set must say `feature_scope`, never `repository_global`.

## Product adjudication

The selected draft direction is option 2: close each registered Codex source/consumer/sink row, block shared legacy APIs from carrying Codex material, and keep adjacent domains as explicit debt. A runtime report records its exact artifact roots and may not describe this feature pass as repository-global. Ordinary activation remains current-owner/current-source only; staged SQL/restore/sync sources may enter only the dedicated staged-import projection and can never be normalized into an ordinary current Provider source.

## Scanner levels

- `contract_schema`: forbidden fields in #35/#55/#41 DTOs, token-free feature draft/public/mutation DTOs, commands and canonical fixtures; Provider-delete, split initial-activation/resume types, resume field allowlist, staged-import exact CAS, typed capture intent, the opaque bridge-only fixed-11-domain coverage receipt, env/common-config/public-Provider/override/diagnostic DTOs are included. Provider-owned delete errors/actions have a distinct closed decoder and cannot enter the secret error/action unions.
- `codex_feature_runtime`: generated canary absent from every registered Codex Provider-primary artifact and public call, including feature drafts/forms/cards, fixed owner/ref usage/model-fetch/coding-plan requests, native-rejected deep links, staged sync, startup history backup, delete preview/confirm, Codex env/common-config ingress, public Provider chain, overrides and diagnostics. Reviewed live sinks remain exact manifest entries, not waivers. MCP `env`/`http_headers` debt is not counted as a Level 2 PASS.
- `repository_static_inventory`: exact AST/path/category entries for known value-bearing source declarations/literals, reported rather than waived. The generator must cover all 127 V7 supplemental path/category entries plus the earlier base register and compare its sorted output to the baseline. Any new/moved `codexMcpEnvOrHeaderCredential` occurrence fails.
- `repository_runtime_global`: `NOT_CLAIMED` until all adjacent credential domains migrate.

Source freeze fails if the generator omits any registered file, reports a new/moved Codex source without an owner/evidence row, conflates a supplemental domain with current-scrubbable `LegacySourceRef`, makes receipt fields externally constructible, changes the receipt or `checked_from_complete_inventory_authority` factory from `pub(crate)`, lets anything except `CodexLegacySourceInventoryBridge` construct `CompleteLegacySourceInventoryAuthority`, fails to atomically bind revision+identity+current expectations+adjacent observations, omits/duplicates/adds one of the fixed 11 domains, derives `inventoryRevision` or `LegacySourceLocationId` from a value, stores a raw path/raw locator/value/value-derived digest inside the receipt, treats an empty collection/count as complete proof, lets startup/summary/capture/Provider-delete proceed without per-attempt bridge revalidation, merges initial activation with resume output, gives resume result data any field set other than exact `stageId|currentResumeCas|status|action|issue`, puts version/command-id/audit or candidate/owner/ref/summary data into it, counts the main resume handler as a sixteenth #35 command, composes external adapter types before owner publication, lets `backend.rs` reference absent external types, omits native locked Rust 1.85.0 `--all-targets` evidence, or still treats an empty secret substitute, checked-in fixture literal, renderer-only value state, masked deep-link preview, pre-gate history backup, fixed consumer, env/common-config source, override/diagnostic reflection, MCP credential occurrence, “legacy” source or backend-only copy as an exclusion.
