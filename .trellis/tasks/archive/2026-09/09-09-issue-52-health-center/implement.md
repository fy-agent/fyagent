# 实施顺序

1. 核对当前来源、锁定方案与 IPC 合同（已完成需求分析）。
2. 实现只读 native 健康快照、严格 TS 端口和状态投影；覆盖每项的事实、失败、未知与不支持。
3. 实现运行状态页、目录直达、可见性约束、单项/批量刷新与既有修复导航。
4. 检查真实数据来源、事件路径、回读与状态改变；完成聚合/边界/隐藏页面/并发/保留结果单元测试及 Rust 只读和归属测试。
5. 运行聚焦检查，再运行完整预归档检查与生产浏览器/串行性能检查；修复引入的问题。
6. 完成本机可获得的原生证据，核对原 dirty worktree 不受影响；更新相关规范与使用说明，提交推送并创建可审阅 PR。交付明确区分源码、浏览器、本机、Windows 和发布证据。

## 责任边界

主控：唯一最终 owner，产品取舍、native service/DTO/command、最终整合、验收和 Git 交付。前端实现者：健康页面、共享 TS 合同/ports/query、导航、浏览器 fixtures 与对应测试。资料员只写来源表。所有人共享隔离工作树，禁止回退他人修改。

## 验证关闭条件

`mise run check:prearchive --exclude-active-task .trellis/tasks/09-09-issue-52-health-center`；`mise run test:browser`；`mise run test:performance`（与全量编译串行）。增加的检查只验证行为与数据边界，不以镜像实现的静态快照代替运行测试。相关最后一次修改后才生成正式验证记录。

## 完成记录

步骤 1–6 已完成；最终最终原生故障恢复、排序高亮、文件与数据库独立回读及原工作树保全通过。产品取舍、实现边界和最后一次代码验证详见 `validation.md`。功能提交 `7bd07e72` 已推送，交付 [PR #187](https://github.com/fy-agent/fyagent/pull/187)，按当前任务流程归档。
