# 历史计划归档说明

本任务按“被替代”结案，不表示 WorkBuddy 的 SuperGrok 订阅接入已实现。

父任务的 2026-09-08 修订明确保留 WorkBuddy 既有 API-key 模型配置，
当前订阅复用交付范围是 Claude Code 和 Codex CLI。拉取模型列表也不能
证明 WorkBuddy 具备订阅调用能力；不新增执行器、不复制 refresh token，
不为满足历史计划而恢复旧认证页面。

本计划存在于原分支 `feat/grok-first-class-iteration` 的
`b8b15dbaf141f7c7fbd7816914fda59a07a2208a`（PR #172）。父任务修订及
PR #185 是替代范围的证据。本轮修复的是遗留任务记录，不扩大产品范围。

原研究、PRD 和未勾选验收清单保留。WorkBuddy 订阅接入及双机真实账号
验收均不标为完成；不据此关闭原产品议题。元数据使用
`resolution: superseded` 和 `historical_acceptance_completed: false`。
