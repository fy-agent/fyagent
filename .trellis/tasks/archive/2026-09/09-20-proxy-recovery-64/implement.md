# Execution

1. Read proxy runtime, managed recovery and reversible-user-config contracts; inspect actual writers and renderer callers.
2. Add real temporary filesystem regressions and minimally guard legacy restore admission/publication.
3. Correct managed partial-restore retry without weakening rebinding checks.
4. Fill actual recovery guidance gaps and run focused renderer/architecture checks plus formatting. Native tests are pending the root integration window.
5. Commit owned changes, report the anomaly matrix and residual runtime evidence, then stop writing.

Owner: /root/installer_27_29, branch codex/next-proxy-recovery-64, base 2c09c4be. Root-provided NEXT_ISSUES.json and VENDOR_SOURCES.md are read-only inputs and excluded from delivery.
