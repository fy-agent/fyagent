# 共享视觉层级与弹窗体验

## 要求

统一桌面排版、间距、圆角和弹窗层级。保留页面布局、导航顺序和操作权限。确认框不再靠重复说明或空 body 撑高；标题、正文和底部按钮尺寸协调。长文案、窄窗口仍可滚动，操作不被遮挡。取消、Esc、焦点恢复和 pending 禁止误关闭保留。

## 验收

有前后实际页面截图/几何证据；确认框、普通对话框、侧栏及至少三个业务页面回归。现有 Radix 和 CSS tokens 是唯一基础，不新增主题框架。V2 types/lint/tests、浏览器回归通过；SPEC 同步和任务归档。
