# Implementation

1. Bootstrap the isolated worktree and read current auth/modular specs.
2. Share and harden the existing Agent session hook and transport identity binding.
3. Add Grok official CLI login/logout handoff at the central auth route.
4. Restrict managed target choices and correct native read-only Grok summaries.
5. Preserve per-target operation results and scope recovery/retry controls.
6. Add focused renderer/native fixtures; run permitted lightweight checks. Record
   deferred native checks exactly and deliver local commit without push/merge.

Do not modify model save handlers, quick setup, credentials/DB/export, installer
preflight, versions or proxy recovery. This agent is not the only writer. Parent
owns integration and final acceptance; actual model/effort unavailable to this
tool context is reported unverified.
