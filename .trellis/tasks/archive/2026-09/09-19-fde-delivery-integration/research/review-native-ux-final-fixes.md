# 最终原生走查补充修复

## 工作区高度

1232×700 原生界面选中项目后，正文只占很小区域，下方留白。浏览器复现测得修前正文 30.11px。根因为项目页未使用现有 `fy-feature-workspace` 角色，shell 未向 PersistentSurface 传递剩余高度。只补页面类名，复用既有布局约束。

Chromium 与 WebKit 的两个窗口共 4/4 定向用例通过：1232×700 正文 292.25px，900×600 正文 198.25px，可滚动到项目设置再返回，没有整页或正文横向溢出。测量及截图在 `~/fyagent/tmp/fde-workstreams-20260919/control-evidence/projects-layout-artifacts/`。这是浏览器布局证据；最终安装另做原生回读。

## 新记录即时状态

原生检查返回后，组件时钟可能仍比新记录早不足一秒，造成短暂待复核。成功返回时以本机 Date.now 更新时钟，再写查询缓存，不信任服务端时间，也不放宽未来时间判定。定向用例修前10/11、修后11/11，证明新成功/失败立即显示且真正未来记录仍待复核。归档项目使用明确只读原因，不再叠加要求重新配置的泛化提示。最终类型检查通过。

## 已完成的真实原生路径

隔离 FYAGENT_TEST_HOME 中新建客户/项目 B，独立说明与 A 切换、未保存草稿取消/放弃、五阶段记录隔离通过。人工登记五个字段跨标签保留，路径输入局部拒绝且可立即纠正，中文冒号/分号/句号保存成功，撤销后保留记录。匹配模型成功后模拟503失败，旧通过失效。B归档后资料保留、修改禁用、预览仍可用。JSON与Markdown导出已回读，没有测试凭据、私有路径或服务端口。

上述人工登记为明确标注的本机演练，不是客户签收。最后的工作区高度/即时状态补丁已在最终构建完成原生复验，结果记录于验收矩阵和交付报告。
