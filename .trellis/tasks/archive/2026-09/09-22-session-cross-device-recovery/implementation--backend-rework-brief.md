> 归档说明：本文的时间与结论保留；具体工作站用户目录已替换为语义占位符。历史路径映射、原稿及提交稿哈希见[归档路径映射](research/archive-path-map.json)。可解析的相对链接已调整。

前轮在协调方明确阶段切换时SIGINT退出130，所有子进程已确认停止，代码已保留且报告未写。不是运行故障或模型不可用，不重复已完成首轮工作。此轮直接回读最新代码落实下面责任范围，sourcecode优先，handoff部分事项已由本轮其它writer解决，勿抢写。root 已落地claim_batch/claim_for_request与schema两字段、preview/export首参数db、nativeinput.target_native_id字段、Codexstoreidentity+retry改造。identity/extract/native三模块worker现真实写入。
继续同一 Session 恢复实现的定向修复，不提交/推送。你的首轮执行完成后才启动本轮。必须先读 codex-implementation-handoff.md 全部最新追加项、qa-interface-request.md 和 backend-report.md。目标是可运行、无数据损失/重复写的集成，不是只让编译通过。

写权限本轮缩小为：src-tauri/src/session_manager/migrate/{restore.rs,reconcile.rs,capability.rs,native/mod.rs,mod.rs,model.rs}，src-tauri/src/commands/session_migration.rs、commands/mod.rs/lib.rs仅接线，必要时test-hooks reexports。不要修改其他文件。root 正在写 identity.rs、receipt.rs、database/dao/session_restore.rs、export.rs、native/codex.rs；独立执行者写 extract/** 和 native/{opencode,hermes,gemini}.rs。

固定协作接口：NativeRestoreInput 新增 pub target_native_id: Option<String>，填入 attempt.target_native_id.clone()；各writer自己负责复用。本轮保留 verify_readback(expected: &MigratableSession) 以避免无益接口大改。root DAO 将新增 AttemptClaim::SourceSnapshotConflict(record) 和 AttemptClaim::RequestConflict(record)，ReceiptStore.claim 会转换成 MigrationError（不返回给restore），restore exhaustive match需保留显式分支作为防御或returnreconciliation；更简单让 ReceiptStore.claim 使用独立三variant receipt::ClaimedAttempt 但须先告知root。推荐DAO返回 MigrationResult<AttemptClaim> 直接表达错误，三variant保持不变——root最终采用此方案，ReceiptStore.claim直接透传。

你的必修项：1全批provider/selection/requestId预检后才有任何写；2retry复用ID；3verify/open/reconcile重新核验当前store与版本；4开终端不得把未经读回的NativeWritten升级成恢复成功；5读回失败保持先前证据不升级；6当前NativeWritePending可能仍是活跃writer，reconcile不能先认无ID为失败再引发第二次写！利用本应用已有single-instance且所有相关入口的同一全局Mutex串行化restore/verify/reconcile（RAII，公共函数外层获取，内部无嵌套锁）或提供同等简单证据，不引入服务/租约框架。
7公共run_with_timeout有整个进程树、stdout/stderr上限和总时限，任何spawn后错误不允许NotStarted。Unix单独process_group+guard终结进程组，通道reader通过recv_timeout不无限join，超限或异常用Unresolved/TimedOut变体且适配所有writer由writer责任者完成（报告契约）。Windows正式elevated app全部用户CLI执行包括--version要在spawn前返回blocked，通过existing windows_runtime条件，不准直接跑。统一probe和writer executable解析，避免GUI PATH缺失/检测错版本；新增 helper provider_cli_path(provider_id) 可复用，必须告诉root和writer。
8注册root已写 commands/session_migration_dialogs.rs两picker；9版本能力矩阵采用新精确证据，Codex appserver原生read/restart及mocknextrequest已证实，OpenCode原生export已证实，Hermes/Gemini新writer由独立者交付后更新不抢写；不把local mock称真实reply。
10完成QA需要的真实生产模块test-hooks，若构造复杂可在restore.rs内部直接加数次调用计数fake writer+inmemoryDB的实质测试覆盖重复调用/未知结果/changedsnapshot/不匹配store/并发，绝不能只测enum和命名。新的单元fixture勿触碰HOME真实目录。

所有终端命令前缀rtk，项目检查只用mise run。先落实接口并写backend-rework-interface.md供并行worker读取，然后完成修复。不要对别人的文件运行全局rust:fmt而造成多writer；最终root格式化。报告准确通过和未通过，写backend-rework-report.md。
接口最终补充（以这里为准）：root 修改 Database.claim_restore_attempt -> MigrationResult<AttemptClaim>，保持原3variants不增加enum；ReceiptStore.claim(attempt)保留给模块测试，生产请用新增 ReceiptStore.claim_for_request(attempt, selected_snapshot_ids: &[String])。这会把整个选择集合及provider/store/workspace/requestKind计算不可变request fingerprint并保存单张表的新列；同requestId换workspace/provider/selection等都在同事务拒绝（IdentityFieldInvalid requestId）。restore_package先构造所有selected snapshot IDs，再传给restore_one和claim_for_request。root负责schema.rs仅增加request_fingerprint列，此文件本轮不在你权限。export::preview_session/export_package也将由root改成首参数 &Database，commands的两个命令请增加State+db_handle并转入db，所有前端wireargs不变。当前有原生subagents在staging等首轮exit才写，不要继续复写他们路径。
批量校验接口进一步明确：root会提供 ReceiptStore.claim_batch(attempts: Vec<RestoreAttempt>, selected_snapshot_ids:&[String])->MigrationResult<Vec<AttemptClaim>>，DB在一个IMMEDIATE事务一次性认领全部候选，任何request指纹/来源新snapshot冲突都回滚且在首次native写前返回。请在restore_package先构造所有proposed attempt，claim_batch拿到对应claims，然后逐一执行。拆restore_one为build_proposed_attempt+execute_claim或等价小函数。这样不能先写A再遇到B身份冲突整调用throw隐藏A。现有claim(attempt)保留给单元测试，claim_for_request也可保留单项调用。新增record.request_fingerprint和record.installation_id是DAO私有字段，不进入前端DTO；此双字段由root负责。所有command-level预检无native sideeffect，批量每项native结果仍逐receipt返回。
额外真实跨包缺陷：resume_command目前只用裸codex/opencode/hermes，可能与provider_cli_path已探测/写入用的版本与store不同。请通过已有terminal安全参数机制，用同一个已验证绝对executable及对应target local store环境打开（Codex CODEX_HOME、OpenCode XDG_DATA_HOME等），不能取包中命令；Windows仍formalclosedhelper限制。如果复杂则open显式拒绝无法证明一致的环境，不能静默启动另一个CLI/另一个store。TargetOpened不能由“终端窗口启动成功”自动产生；它只证明请求打开，保留既有nativeReadbackVerified和返回true，native真正加载证据/用户自报分别保留。

writer最新契约：GeminiWriter现新增 native/gemini.rs，即将落盘，请注册模块、writer_for("gemini")及resume命令；verified_write_versions=0.46.0。OpenCode1.18.30 Hermes0.20.5。native三者都将依赖pub(crate) fn provider_cli_path(provider_id:&str)->Option<PathBuf>。默认PATH选中Gemini npm-global0.59应明确版本不支持，不可偷选另一安装旧版本。stage能力release只标真正证实，Gemini不调用会推理的list-sessions，而固定安装导出helper读取本地数据无推理。配置shell打开必须sameexecutable/store。ROOT已发布package.reject_duplicate_keys供extract复用。你先处理接口编译阻塞，随后orchestrator单元测试。

Root integration observations (after common resolver landed):
- Codex now uses provider_cli_path + user_cli_execution_blocked; keep common signatures stable.
- Native worker compilation caught model.rs test still calling deleted sanitize_stderr_tail (~846); please clean test as model owner.
- Common run_with_timeout drain recv_timeout must not report Exited(0) with default/truncated output on reader failure or cap; postspawn uncertain output => TimedOut/unknown.
- Mac processgroup cleanup exists, Linux equivalent absent. Supported product surface is Mac/Windows, so do not expand scope to Linux if not supported; ensure supported process tree cleanup and keep formal Windows block before --version.

Native agent integration finding: output cap shared version/readback runner is 1 MiB. Full transcripts can exceed it: either parameterized bounded readback output sized to package cap, or explicit prewrite size admission. Never silently truncate and count it as successful Exited/readback. Postspawn reader cap/error must be unresolved.

Platform integration: repository forbids implicit cfg(unix), cfg(not(macos)) fallthrough. Please native/mod common runner use explicit macOS/Windows positive targets + fail-closed unsupported fallback. No Linux support claimed. Root changed native child modules cfg(unix) -> macOS; identity owner removes Linux paths. Scanner agent handles reviewed exact allowlist hashes.
