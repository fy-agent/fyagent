继续Grok4.7 high独立评审，不修改代码。R同前。此轮冻结审查范围是：migrate/{identity.rs,identity_platform.rs,receipt.rs,export.rs}、database/dao/session_restore.rs、database/schema.rs中本次session_restore表。root已写完这批，其他backend restore/reconcile/commonrunner与nativeproviders/extract仍各owner修复，本轮不要把他们的进行中缺口当最终结论（可以报告跨接口关键依赖）。
对照后端session-migration规范：复制配置不复用回执；同设备provider原生store替换不能错误去重；request ID绑不可变整个选择和目标；一批所有claim事务验证后才nativewrite；同origin新snapshot默认冲突，明确另存新副本例外；同provider/store/nativeID精确映射再导出继承origin，而不是按正文猜来源；单张SQLite无消息正文副本。root新DAO两字段installation_id/request_fingerprint为同一次action跨store不可变绑定，查是否真有必要/是否已有字段足够/有哪些可删复杂性，必须用具体不变量反例评而不是喜好。安装身份原子文件锁/损坏拒绝/机器user绑定也审当前实现实际漏洞。
给P0/P1/P2具体触发、fileline、影响和最小修改，区分已确认与待验证，关注冗余。不要重写整套设计或建议远端同步框架。源码与合成测试即可，不读真实secret/用户history，不跑真实推理；命令rtk，项目测试mise但多writer编译中间态不作为实质reviewfinding。报告I/grok-identity-review.md。