Active task: /Users/serendipity/Documents/Codex/2026-09-22/new-chat/work/fyagent-session/.trellis/tasks/09-22-session-cross-device-recovery
继续原session前端实施，上一轮进程已结束。协调者回读发现报告把部分未完成内容称为100%闭环，请本轮只据实际产物报告。原允许路径全保留，只有你写前端src/pages/sessions与DTO/port/wiring，其他backend/QA/构建writer不动。构建已由独立执行者修复budget为637815，不再报告670259为当前问题。导航单测也已通过33项。所有项目验证mise run且rtk。
本轮只修确切缺陷，并新增可供QA测试的纯helper到src/shared/features/session-migration.ts（一个语义owner，避免页面export触发fast-refresh）：
1. ImportPackageDialog handleExecuteRestore 每次点击crypto.randomUUID严重违反重试幂等。一个确认动作的requestId固定，超时/异常再试同一绑定必须复用。只有用户明确改变package身份、provider、所选snapshots、targetWorkspace或requestKind才新建请求。请求期间禁用关闭/切换/修改；异步结果不得污染已关闭或新打开弹窗。遵循useDialogState生命周期规范。
2. ImportPackageDialog完全没有localProbe门控，取第一来源provider却提交全部snapshots，多来源包必然错配。增加目标软件选择（只能包内来源一一对应）与会话选择/摘要预览，按目标provider筛选snapshotIds；probe installed && writeSupported后才能点击确认，显示失败/未验证原因。失败返回/ambiguous/pending/needsReconciliation绝不能统一绿色成功；真实stage中文状态。
3. Page.sessionsToExport多选时只preview当前selectedSession，实际handleExport却发所有勾选，存在未预览内容被导出。打开导出对话框时对冻结的全组选项逐个严格preview，任何失败列具体会话并阻止整包导出；预览和export使用同一冻结item集合，不在点击保存时重新读活的selection。提供待加载/失败/完整成功状态。
4. 正确provider ID是grokbuild（来自src-tauri/src/session_manager/mod.rs），SUPPORTED_PROVIDER_IDS和标签现在是grok，导致假未安装/无法筛选。统一grokbuild，StartupGuide也相应核对。
5. structured errors native返回常为JSON string；formatMigrationError现在直接展示整串。应在port或shared feature单一owner解析code/detail并映射易懂错误，不把原始内部JSON/代码黑话当正常产品文案。完整的列表/恢复记录/探针加载失败应显示失败，不能默认空数组制造无会话。
6. ImportPackageDialog仍然只能手输文件，选目录按钮假冒选文件。新增SessionMigrationPort.pickPackageFile():Promise<string|null> 与 pickExportPath(defaultName:string):Promise<string|null>，桥接分别invoke pick_session_package_file / pick_session_package_export_path {defaultName}；协调者负责后端对应两个dialog命令。导入实际选择json文件，导出实际保存文件（不是拼接目录路径）。Port返回unknown严格parse，浏览器rejectNativeOnly，tauri懒桥接。必要按钮文字“选择文件”“选择保存位置”。不要复用config pack picker，它返回已读JSON正文不是path；SQL picker过滤也不适用。
请协调保持provider目录和既有Memory编辑不变。最后 mise run typecheck/lint/test:unit tests/session-migration，自检报告只列真验证，最终browser由QA。报告中区分已实现/未验收，不说生产级100%。