Active task: /Users/serendipity/Documents/Codex/2026-09-22/new-chat/work/fyagent-session/.trellis/tasks/09-22-session-cross-device-recovery
实际目录 /Users/serendipity/Documents/Codex/2026-09-22/new-chat/work/fyagent-session。origin=https://github.com/fy-agent/fyagent.git，基线 origin/main c0b2ec21；隔离分支 codex/session-cross-device-recovery-20260922。用户已明确批准实现直到完成并提交PR，无需重复审批。你是执行子Agent不再转派。先读本任务prd/design、implementation/wire-contract.md和本仓库对应spec；准备包原来src/v2、src/lib路径已过时，当前只有src/pages、src/shared、src/domain。所有终端通过rtk，项目验证用mise run，环境由协调方bootstrap，不自行改锁或工具版本。所有操作仅此worktree，禁止操作用户真实会话、凭据、发推理费用、发布提交/PR。只改允许路径，发现跨包需要写出说明。其他writer并行，不覆写。用当前文件与证据，不把文档当已实现。提供实际修改、自检、失败和未解决项；产物写本任务implementation/各自-report.md。关键决策需Jev可写request交协调者，给完整有限候选和事实。
你使用 Antigravity gemini-3.8-flash-high high，是UI/UX与前端实现负责人。先读 ~/.codex/DESIGN.md 与当前前端spec；参考 deliverables/session-prototype.html 和 ux-spec，但必须融入当前生产单renderer，不能把原型当代码照搬。只写 src/pages/sessions/**、src/shared/features/session-migration.ts、src/shared/platform/tauri/feature-ports/sessionMigration.ts，以及接线必需的shared/features/ports/provider和platform桥接、src/app/PersistentPrimaryOutlet.tsx/navigation相关、SideNavigation必要图标；允许你为旧memory增加辅助路由/入口但不改其编辑语义和文件。路由让Sessions取代Memory一级导航，旧memory保留二级可访问，其他如Projects保留(由独立PR196负责删除)。跨文件接线以当前查找路径为准；写报告准确清单。
实现真实可操作Session列表/搜索筛选、内容预览、严格导出/导入预览、目标目录选择、重复默认跳过与明确另存副本、阶段/错误反馈、在目标软件打开与用户自报区分，所有命令按公共合同。7provider可见且按后端能力禁用，未知不能假绿。浏览器preview应native-only，无演示数据污染。用现有shared primitives/search/list/dialog/FeaturePorts，严格unknown解析/不要any或新UI库。QA并行写tests，尽快落公共TS类型。只改责任文件，不提交。完成 mise run typecheck/lint适用检查，能跑页面验证则报告具体层级。
