Continue same Cursor GPT5.6Sol High QA. Production testhooks now exist at fyagent::migration_test_hooks (see lib.rs~90) exposing production extract/identity/model/package and restore_with_writer; actual crate name inspect Cargo.toml. Bounded exclusive write: src-tauri/tests/session_migration_model.rs and optionally a new src-tauri/tests/session_migration_orchestration.rs; reports I/qa-production-rust-*. No other production files or frontend changes (root just fixed written/readback UI presentation). Goal: remove path-include fakecrate clone/stubs from existing integration harness; import REAL compiled production modules via test-hooks, preserving focused 47-ish acceptance tests and synthetic isolated homes. Do not reinstantiate local module trees or broaden test-only override to release. with_local_state_dir must remain test-hooks available. Opus is finishing testhooks/restore internaltests concurrently; don't touchlib/mod, report compile interface blockers. Real orchestrator fake countedwriter tests already in restore.rs owner; inspect them before adding externaltests, no duplication. At least verify existing harness tests actually execute productioncrate functions, no userhistory/credentials/realinference. Run only canonical rtk proxy mise run rust:test session_migration_ after harnessupdate (no whole suite; root final will). All commandsprefixrtk, no commits. R origin fy-agent/fyagent branch codex/session-cross-device-recovery-20260922 mainc0b2ec21. Writes previouslyownedexternalQA allowedrootreleased. Output I/qa-production-rust-report.md with actual testcasecount and compile errors/success, release all ownership then exit. Logs actualmodel. No WindowsUAT.
