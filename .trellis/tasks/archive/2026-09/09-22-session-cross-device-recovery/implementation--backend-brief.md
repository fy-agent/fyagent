> 归档说明：本文的时间与结论保留；具体工作站用户目录已替换为语义占位符。历史路径映射、原稿及提交稿哈希见[归档路径映射](research/archive-path-map.json)。可解析的相对链接已调整。

Active task: /Users/<username>/Documents/Codex/2026-09-22/new-chat/work/fyagent-session/.trellis/tasks/09-22-session-cross-device-recovery
实际目录 /Users/<username>/Documents/Codex/2026-09-22/new-chat/work/fyagent-session。origin=https://github.com/fy-agent/fyagent.git，基线 origin/main c0b2ec21；隔离分支 codex/session-cross-device-recovery-20260922。用户已明确批准实现直到完成并提交PR，无需重复审批。你是执行子Agent不再转派。先读本任务prd/design、implementation/wire-contract.md和本仓库对应spec；准备包原来src/v2、src/lib路径已过时，当前只有src/pages、src/shared、src/domain。所有终端通过rtk，项目验证用mise run，环境由协调方bootstrap，不自行改锁或工具版本。所有操作仅此worktree，禁止操作用户真实会话、凭据、发推理费用、发布提交/PR。只改允许路径，发现跨包需要写出说明。其他writer并行，不覆写。用当前文件与证据，不把文档当已实现。提供实际修改、自检、失败和未解决项；产物写本任务implementation/各自-report.md。关键决策需Jev可写request交协调者，给完整有限候选和事实。
你使用 Cursor claude-opus-5-thinking-high，负责实质后端实现：严格迁移包、七来源final-only投影、来源/快照身份、现有SQLite回执、能力探测、原生写入/读回/崩溃对账、Tauri commands接线及必要Rust测试。只允许写 src-tauri/src/session_manager/migrate/**（排除native/codex.rs，协调方独占），session_manager/mod.rs仅注册模块，src-tauri/src/commands/session_migration.rs、commands/mod.rs、lib.rs仅注册，database/mod.rs/schema.rs/backup.rs及新增专属DAO。不改无关业务、不大规模格式化。读取 research/session-recovery-20260921，当前schema已24不能照旧20。保持一张本机回执表，不引入服务框架、第二数据库、将正文存回执等冗余。
请先快速落model.rs与Codex集成接口，再推进其他部分。你承担真正功能代码，不只方案。7家writer必须由真实CLI格式/当前官方代码证据支持，不能猜测或调用真实用户会话；必要时先完成已验证OpenCode/Hermes，清楚剩余依赖，但不要自行宣告整包完成。Codex由协调方解决。fixture与纯测试由你自行添加；QA独占src-tauri/tests/session_migration*所以你单元测试放模块内部。最终执行 mise run rust:fmt、mise run rust:test session_manager::migrate（等环境就绪）。整包较大允许持续工作直到可集成，不做PR/commit。
