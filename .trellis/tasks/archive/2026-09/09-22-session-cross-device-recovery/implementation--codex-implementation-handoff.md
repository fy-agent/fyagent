> 归档说明：本文的时间与结论保留；具体工作站用户目录已替换为语义占位符。历史路径映射、原稿及提交稿哈希见[归档路径映射](research/archive-path-map.json)。可解析的相对链接已调整。

# Codex adapter implementation now ready for registration

Coordinator owns native/codex.rs; it now implements CodexWriter:NativeSessionWriter, matching the published trait. Please backend owner register pub mod codex and writer_for("codex"), update native registry tests and capability matrix to `codex.legacy-rollout.v1`. Scope of tested version is0.154.0, fresh official thread/read after restart visible, next-turn local mock request exact. See coordinator-codex-findings.md and evidence/codex-native/. Root no longer relies on inject-only, so old instructions §§3.1/3.3/4 are superseded: allocate UUID locally, persist receipt ID before atomic new-only nativefile; emit official legacy event+response projection. user.text + assistant final phase enable strict reexport without trusting arbitraryunknown kinds.

Evidence also covers consecutive users and missing final: native UI/API correctly reports interrupted rather than fabricated final. Adapter uses read-only app-server for verification, never turn/start or paid inference in product.

Windows formal release already prohibits launching user-owned CLIs elevated. Adapter preserves that boundary and reports unavailable before native write. This is an unresolved integration prerequisite (ordinary-user helper), not a permanent product-scope cut. Need final PR call this out unless solved. Windows authentic remote readiness is blocked token mismatch, so no actual Windows UAT claimed.

QA needs test-hooks narrow reexports (qa-interface-request.md), please provide if practical. Environment bootstrap and base rust:check passed. Avoid full checks until compilation integrated, later run relevant single set.


## Integration review follow-up (coordinator, 2026-09-22)
- NativeSessionWriter gained verified_write_versions; coordinator has now implemented `["=0.154.0"]` on Codex. Please read this file before compiling/registering.
- Current capability/extractor comments about inject-only being blocked are superseded by the isolated legacy rollout proof. Unknown metadata must still fail; change stale naming, not that guard.
- Existing formal Windows runtime security forbids all elevated user CLI execution, including `--version`, OpenCode import/export and Hermes import. Preserve fail-before-spawn in common runner and capability; do not silently bypass because Windows UAT was waived. Host helper scope is a separate actual product integration gap.
- Current OpenCode writer store ID is only a path hash, contrary to the trait's identified-instance requirement. Do not claim target-store verified on pathname alone: use the existing local identity registry + provider-owned or file identity evidence; if store replaced, invalidate receipts and require new identity. Cross-device backup of local receipts also must not impersonate current device.
- Current `run_with_timeout` drains output unbounded; kills only wrapper then joins reader threads (possible descendant pipe hang); `try_wait` error after spawn is not ProvenNoSideEffect. Bound output, bound process lifetime/whole process group, preserve unresolved status after any spawned unknown outcome. No need generic framework.
- OpenCode/Hermes writers alone with no same-provider extractor cannot complete any normal export→restore flow. Please supply fixture/source-backed extraction for these two and concrete gaps for other four. Extra Luna source evidence is being gathered to `remaining-provider-evidence.md`.
- QA requested injectable orchestration/test-hooks in `qa-interface-request.md`; tests must exercise actual receipt behavior, not only source matching.

- Identity review: install_identity is only a random ID plus boundPath. Copying a FyAgent backup/home to the same path on another Mac copies that identity; requirements need a native device binding, not path-as-device. Reuse existing machine identity owner if available, bind install state to OS-machine/user identity hash (not export). Unknown must fail closed, not silently trust copied receipts. Protect concurrent reads/writes to namespace state: per-process parallel previews can otherwise mint/drop namespaces.
- Preview/export of a restored native session must inherit original originId from an exact current-device+targetStore+provider+targetNativeId receipt mapping, regardless of new content digest; then recompute snapshotId. Do not match by body alone. Otherwise unchanged round-trip re-export becomes a different origin and bypasses semantic duplicate handling; continuation should change only snapshot, not origin.

- Native-continuation executor independently confirms installed Hermes 0.20.5 has `hermes sessions export <file> --session-id <id>`. Current HermesWriter comment saying no native read channel is contradicted. It is now running isolated import→official export; see native-continuation-report.md when ready. Wait for concrete result then implement readback instead of leaving blocked.

- Coordinator added commands/session_migration_dialogs.rs (root-owned, complete). Please register module/reexports and Tauri invoke handlers for `pick_session_package_file()` and `pick_session_package_export_path(defaultName)` alongside migration commands; frontend is now implementing these real JSON path pickers. These only return paths, no file reads/writes; migration commands keep validation authority.
- Grok 4.7 independent Codex review complete in grok-codex-review.md: no P0/P1. Root fixed P2 exact-executable runtime version gate before receipt/write, preserves whitespace-only assistant final per official source while refusing empty final and whitespace-only user. Unknown readback error intentionally remains unresolved, never implies write-free/retry. Root added native encode→strict extractor round-trip test.

- Strict extraction review: classify_user currently if ANY content_item_kind is runtime, drops the whole message even when other blocks are user.text. At minimum fail mixed/unknown classification instead of silently dropping real input; preferably use official aligned per-block provenance when length/shape matches, retaining only exact user-text blocks and counting runtime omissions. No heuristics and no silent content loss. Rule provenance/source version must not be inferred only from current installed CLI; old transcript headers may differ even with a newly upgraded binary. Read verified source cli_version when present, or reject ambiguous version.
- Probe currently runs plain `Command::new(binary)` while writers resolve custom/home paths. A GUI PATH difference can show not installed, or probe one executable and write through another. Use one existing provider CLI resolution owner for version and mutation (root Codex now verifies its actual executable just before write, per Grok).

## Integration defects found after initial restore.rs review (must fix, not report-only)
- Preflight ALL selected sessions/provider consistency before the first receipt/native mutation; currently a mixed provider package can write an earlier item then throw on a later one and hide partial results.
- SourceSnapshotConflict exists only as an enum: atomically reject defaultImport of a changed snapshot for the same origin+provider+store+device. SaveAsNewCopy is explicit exception. Add concurrency/changed-source tests.
- ExistingRequest must validate immutable request intent (workspace/provider/store/requestKind/snapshot set) instead of executing a new workspace using the old receipt.
- Add an optional preallocated target_native_id to NativeRestoreInput. Failed/proven-no-side-effect retries must reuse the recorded ID, never mint a new ID that DAO then refuses to replace. Coordinate interface changes with root Codex writer.
- verify_native_readback/open_restored_session must re-resolve the current provider store and compare receipt targetStoreId; row-own stored hash is insufficient. Version/OS gates must still apply.
- open_restored_session currently allows NativeWritten and sets TargetOpened (which is_restored treats as success), bypassing native readback. Require verified native history, and terminal spawn is an open request rather than proof of loaded target. Do not promote unverified receipt.
- apply_readback on error always sets NativeWritten even from Pending/NeedsReconciliation/verified. Preserve existing proof on inconclusive read errors; mismatch/NotVisible become reconciliation; never promote unknown native write to written from a failed read.
- expose narrow existing test-hooks interfaces for isolated Database + fake counted native writer orchestration. QA needs actual production orchestration not source-clone tests only; details qa-interface-request.md.
- extraction-source-evidence.md now provides exact OpenCode1.18.30 and Hermes0.20.5 final/provenance signals; native-continuation-report.md provides native preservation defects and verified remedies. Separate provider writers will be assigned after you relinquish initial ownership.

## A2A phase handoff request (coordinator)
Your initial rust test/clippy pass is received. Please finish the current bounded fixes and write backend-report.md, then return to release your initial broad file ownership. Do NOT try to absorb every new item in this evolving handoff: independent implementation packages are ready in staging and need your initial execution to end before landing. Next attempt will resume this same Cursor session with backend-rework-brief.md and narrower paths. Root owns receipt/DAO/export/Codex; native providers, identity hardening and strict extraction each have staged implementations. This is an explicit dependency handoff, not permission to claim them implemented or to overwrite their work.
