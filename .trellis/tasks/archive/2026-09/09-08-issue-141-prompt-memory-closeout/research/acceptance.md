# 两项 P1 分支验收

日期：2026-09-08。修复提交：`c6303266b41e96e16322b065a379e097a0835ffd`，基线：`2f264d2f89326601a33f610c72a9f0143306d066`。

## 结论与范围

本分支修复并完成 macOS 原生复验：未启用 Prompt 的库操作误清空生效文件，以及无效日记文件导致整个 Daily Memory 列表失败。修复位于既有两个后端模块，复用现有写入和恢复机制；没有新增 IPC、迁移、依赖或并行实现。

这里只验收两个具体缺陷及其兼容行为。未合并、未发布；不关闭 #141 汇总票，不替代 Windows 原生验收，也不宣称完成其余诊断 UI、事务系统或其他 finding。

## 自动检查

- 首轮 10 项回归在旧实现上 6 失败、4 通过；修复后 10 通过。
- 独立审阅补充同一时间值的导入碰撞回归，旧 ID 逻辑 1 失败；使用现有 UUID 后最终 11 项全部通过。
- 完整归档前检查通过：前端 179 文件、1,582 通过、1 跳过；Rust 主库 3,197 通过、5 忽略，其余 workspace/integration suites 通过；合同测试 616 通过、1 跳过；native-fetch 4 通过。格式、类型、lint、Rust check/Clippy、锁文件与任务合同通过。
- 完整入口：`mise exec -- uv run --locked --no-sync mise run check:prearchive --exclude-active-task .trellis/tasks/09-08-issue-141-prompt-memory-closeout`。首次未激活项目 Python 时因 `spawnSync python ENOENT` 退出；激活仓库受控 Python 后整套重跑通过，没有修改系统 Python 或绕过检查。

## 分支构建与原生操作

通过 `mise run build:debug` 构建本提交；首次 DMG 步骤失败，保留记录，原入口重试生成应用与 DMG，退出码 0。产物属于本机调试构建，不代表正式签名、发布或安装验收。原生复验二进制 SHA-256：`91420b6aadf2887a65eb93333b36d891e4d2b9c0357720ea232961e0a5855a8c`，与该次受控编译输出一致。

运行前核对精确可执行文件、`FYAGENT_TEST_HOME` 和进程实际打开的数据库均指向隔离资料。所有测试正文为合成数据。

| 场景                     | 原生操作与回读结论                                                                                                                                              |
| ------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 混合日记目录             | 合法普通日记和闰日日记显示 2 条；README、非法日期和日期同名目录被忽略，合法内容均可读。                                                                         |
| 日记搜索                 | 共同中文词返回 2 条，LEAP 返回 1 条，清除搜索恢复 2 条；重新进入页面仍正常。                                                                                    |
| 合法日记保存             | 在原生编辑器保存后，文件内容匹配输入，恢复副本等于前像；无效文件、其他日记及长期记忆摘要和修改时间不变。                                                        |
| 未启用 Prompt 新建与编辑 | 数据库分别新增、更新对应 disabled 记录；生效文件、备份和撤回记录的内容、大小与修改时间完全不变。编辑器原有尾部空白规范化保持。                                  |
| 从文件导入               | 新增 disabled 记录，内容摘要等于生效文件；生效文件与恢复记录不变。                                                                                              |
| 启用后再次导入           | 新增独立 disabled ID，原记录保持 enabled，生效文件和恢复记录不变。同一时间值的碰撞由确定性单测另行覆盖。                                                        |
| 已启用内容编辑与显式停用 | 编辑保存正常同步，备份保留前像；明确停用后生效文件按原语义清空，备份保留最后启用内容。                                                                          |
| 环境恢复                 | 测试进程退出，日常安装版与正常数据库恢复，未携带测试 home，返回原账号页面；真实 Claude/Codex Prompt 与 OpenClaw Memory 的内容摘要、大小和修改时间均与前置相同。 |

本机快照复核脚本的 12 项断言通过。GUI 浏览、搜索、输入、保存和切换由 CUA 原生应用操作完成；文件与数据库回读独立于界面提示。截图只留在本次 CUA 历史，没有声称生成独立截图文件。

## 证据与恢复

完整日志、合成资料快照、进程与构建回执仅保存在本机外部证据包 `fyagent-issue141-fix`（2026-09-08），不把用户路径、原始日志或截图提交到仓库。主要回执：`full-prearchive-managed-python.result.json`、`native-build-retry.result.json`、`native-app-artifact.json`、`native-isolation.json`、`native-verification.json`、`native-restoration.json`。

修复差异取自 `c6303266`。后续任务资料已经归档，最终回退方式收敛为：仅对两个 Rust 源文件、现行 Prompt/Memory 合同、开发文档索引和新增维护说明这五个文件生成该提交的反向补丁，保留归档证据。已经在交付分支用 `git apply --reverse --check` 只检查，退出码 0，未实际回滚。执行回退时应在新分支应用该范围补丁、重跑检查并提交；无数据库迁移，不需要数据降级。完整五文件清单与反向检查回执存于外部 `rollback-dry-run.json`，补丁存于 `scoped-fix.patch`。运行回退已实际完成：退出测试构建，重新打开原安装版。测试资料不混入交接目录。
