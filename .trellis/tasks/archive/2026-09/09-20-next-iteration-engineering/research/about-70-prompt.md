Active task: ~/.codex/worktrees/fyagent-next-night-20260920/fyagent/.trellis/tasks/09-20-next-iteration-engineering

Implement GitHub Issue #70 in fy-agent/fyagent. Your only writable code checkout is ~/.codex/worktrees/fyagent-next-about/fyagent, branch codex/next-about-70, baseline 2c09c4be2c5f50b4060fd6f7a13a7be31c364c54. Other agents are working elsewhere: do not revert their edits or edit the original ~/fyagent checkout. The user explicitly authorized implementing all code-solvable Issues tonight; do not stop for another planning approval. You may create a narrow Trellis task in your checkout if required, but keep the implementation focused.

Issue acceptance: add a concise About entry in the existing app shell; show a plain product introduction, actual current version, update and support links; reuse existing release metadata with details shown on demand; content must match actual version and valid links.

Current TopBar.tsx contains Brand and ThemeToggle; Brand is noninteractive. Implement a modest accessible About dialog reachable from the existing top bar. Preserve the current design, keyboard focus and Escape/cancel behavior. Read ~/.codex/DESIGN.md and relevant frontend specs. Reuse existing primitives, external link/open ports and canonical version source; do not hard-code 0.4.6 or create duplicate metadata authorities. Do not automatically fetch or apply updates on dialog open. Human copy must be plain Chinese. Do not expose execution/audit jargon in the interface.

Ownership: src/widgets/app-shell/TopBar.tsx, new About-related component/style files, any narrowly needed existing platform/version adapter, related renderer tests, a focused About spec and your task evidence. Do not modify SideNavigation.tsx, router/navigation state, shared shell.css, version numbers, dependency lockfiles, release workflow, provider/credential/proxy code, or existing 0.4.6 upgrade fixes. If a required change falls outside this boundary, report the exact dependency instead of modifying it.

Use rtk-prefixed terminal commands. Repository checks use mise run (read .mise/tasks/frontend.toml). Bootstrap frozen dependencies if needed; don't share mutable node_modules with another checkout. Run meaningful focused tests for opening/closing/focus, actual version and links; run typecheck, lint and formatting checks. Avoid the full browser/performance suite because the coordinator will run combined gates. Self-review the diff. Commit only owned changes with a Conventional Commit; do not push, open/merge PRs, close Issues, or send messages outside this local task.

Deliver a concise report at .trellis/tasks/<your-task>/research/result.md with commit SHA, files, exact check results and failures, residual acceptance requirements. Report the actual model/provider from the runtime when observable and do not infer account quota. Stop after focused implementation and verification, or with a concrete blocker and all partial artifacts preserved.
