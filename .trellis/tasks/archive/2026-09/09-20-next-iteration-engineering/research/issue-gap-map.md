# Research: Open Issue gap map

- Query: Map the merged 17 open Issues (#25, #27, #29, #34, #35, #40, #42, #43, #47, #56, #61, #64, #67, #68, #70, #73, #92) to the 0.4.6 integration candidate at `2c09c4be2c5f50b4060fd6f7a13a7be31c364c54`, identify real code gaps and executable work packages.
- Scope: internal
- Date: 2026-09-20

## Findings

The current checkout already contains the large contracts delivered by the recent 192/193/194 integration line: typed agent catalog/readiness and lifecycle, shared change-plan/apply workspaces with rollback/readback, deep-link risk parsing, native Secret backend primitives, managed auth, and release preflight/sealing. Those are evidence of implementation, not proof of user acceptance, signed release, named operational ownership, or a physical-device run.

Evidence anchors: `src/shared/features/agent-install-readiness.ts:3-20,133-186` (closed readiness/reason codes); `src-tauri/src/agent_install/lifecycle_policy.rs:10-128` (closed action/surface policy); `.trellis/spec/frontend/change-plan-workspaces.md:79-181` (plan identity, secret lifetime, writer-owned rollback); `src-tauri/src/config/recovery.rs:203-273,317-415` (backup and external-change guard); `src/domain/configuration/serialization/deeplinkRisk.ts:1-200` and `src-tauri/src/deeplink/parser.rs:1-240` (untrusted import boundary); `src-tauri/src/services/secret/types.rs:1-180` and `src-tauri/src/services/secret/platform/{macos,windows}.rs` (platform secret backend); `.github/workflows/release.yml:596-1128` (Windows preflight/signing path).

### Issue-by-issue map

| Issue | Implemented evidence | Remaining codable item | Non-code / physical prerequisite | Suggested work package / owner | Minimum sufficient validation | Code-only close? |
|---|---|---|---|---|---|---|
| #25 official source and licence links | Catalog entries have `officialLinks` (`src/shared/features/agents.ts:85-98`); native sources carry `official_page` and allow-listed download hosts (`src-tauri/src/agent_install/sources/mod.rs:80-95`, `qoderwork.rs:11-85`, `opencode.rs:22-114`). | Render homepage, download and licence links in the software detail card; expose closed link kinds and verify every seven catalog entry has all required links. | Link review/update is product/content maintenance; links must be checked against current vendor pages. | `catalog-provenance-ui` / frontend catalog owner, with source owners updating metadata. | Renderer test asserts all supported entries render labelled links; source tests continue to reject non-official hosts. | Yes, after metadata and UI tests; ongoing link freshness remains operational. |
| #27 pre-install checks and actual change | Readiness/inventory already models platform, target, permissions, source and revisions (`src/shared/features/agent-install-readiness.ts:218-230,291-344`); lifecycle UI has target picker and preflight/job stages (`src/pages/agents/AgentDirectory.tsx:117-148,244-250`; `src/pages/agents/useAgentLifecycleAction.ts:...`); browser test covers readiness fixture (`tests/browser/agent-directory.spec.ts`). | Add a single preflight summary showing platform/arch, disk, writable target, runtime/source, destination and exact expected change, then bind confirmation to the same opaque target revision. Existing Codex path should be reused rather than another scheduler. | Real disk/permission/UAC and runtime checks require native OS runs. | `install-preflight-summary` / agent directory + native readiness owner. | Fixture tests for each blocking reason and target revision invalidation; one local install job proves preflight-to-job identity. | No for the full acceptance; UI/contract portion yes. |
| #29 supported install path identity/helper behavior | Helper protocol/action is closed and heavily tested (`tests/codexUserHelperContract.test.ts:163-249,1009-1024`); lifecycle reason codes include scope, authorization, helper and rollback failures (`src/shared/features/agent-install-readiness.ts:151-185`). | Audit every current agent source against current-user vs all-users target, ensure helper accepts only closed path/action, and surface partial result/recovery in the shared job result. | Windows/macOS user identities, admin denial, helper signing and real installation locations. | `install-identity-recovery` / native installer owner. | Native fixture matrix for each path and helper rejection; one current-user and one denied-elevation run per supported OS. | No. |
| #34 first-use recommendation reaches usable configuration | Purpose chooser and recommendations exist (`src/pages/agents/FirstUseGuide.tsx:22-159`; `firstUseRecommendations.ts:6-43`), with browser coverage of choices, keyboard, persistence and no accidental install (`tests/browser/first-use-guide.spec.ts:25-150`). | Recommendation cards currently only show text and then “view all software”; add deterministic navigation to the selected agent's actual configuration/install action while preserving skip/all-software and #47 keep/replace handoff. | A real selected-agent flow needs installed/absent configurations and per-vendor external handoff checks. | `first-use-direct-entry` / frontend agents owner. | Browser test selects each purpose, opens target config, asserts no model request, and verifies existing-config branch. | Mostly; final vendor flow is not. |
| #35 Provider SecretRef migration | Native SecretRef types/backends and redaction boundaries exist (`src-tauri/src/services/secret/types.rs`; `src-tauri/src/services/secret/{mod.rs,memory.rs}`); provider live config still reads/writes `apiKey` and raw settings (`src-tauri/src/services/provider/live.rs:675-719,1142-1175,1910-1998`; `src/pages/models/Page.tsx:1087-1100,1534-1603`). | #35 is reserved for its implementation owner: locate the first production Provider consumer, add migration/backfill/read/update/revoke and Codex writer path, then prove no key in logs/exports. This map intentionally does not design the migration. | Keychain/Windows Credential Manager availability, existing-user migration fixtures and external tools that require plaintext. | `provider-secretref-migration` / #35 owner; this report only supplies interface dependencies. | Contract tests for SecretRef DTO redaction, migration failure preserving old config, Codex save/readback, missing/locked secret. | No. |
| #40 unified compatible API config/presets | Quick setup validates URL/key/model and creates closed target requests (`src/pages/models/quickSetup.ts:1-145`); provider presets include Codex/OpenAI-compatible and coding-plan modules (`src/domain/configuration/presets/*.ts`); provider capability tests exist (`src/domain/configuration/serialization/providerCapabilities.test.ts`). | Add explicit Aliyun API-vs-Coding-Plan, Tencent and Ark presets with distinct endpoint/key/protocol semantics; ensure editor displays protocol-specific fields and active connection check uses the selected mode. | Credentials and vendor endpoints must be verified against current official API behavior. | `provider-preset-matrix` / provider configuration frontend + native provider owners. | Pure preset/capability matrix tests, then opt-in connectivity tests with appropriate credentials. | Partly; real connectivity cannot be code-only. |
| #42 apply one source to supported Agents | Managed auth models account/connection consumers and provider binding (`.trellis/spec/frontend/managed-auth.md:74-190`; `src/pages/auth/ConnectionsView.tsx`); provider live sync supports multiple app targets (`src-tauri/src/services/provider/live.rs:1370-1472`). | Build a batch apply plan over currently writable targets with independent preview/save/readback/recovery and retry per failed target; avoid treating shared provider storage as proof each target consumed it. | Current target permissions and real config formats for each Agent. | `multi-target-provider-apply` / provider live + change-plan owner. | Unit tests for independent result rows/partial failure; fixture readback for every supported target. | No, because service readback is required. |
| #43 official login/expiry/logout | Auth page, login sessions and agent auth observation/session hooks are present (`src/pages/auth/{Page.tsx,useManagedAuthLoginSession.ts}`; `src/pages/agents/{AgentAuthStatusPanel.tsx,useAgentAuthSession.ts}`; `.trellis/spec/frontend/agent-auth.md:105-223`). | Fill the matrix of actual current official flows, especially Grok official login vs xAI device-code vs API key labels, with expiry/exit/recovery UI per provider; reject unsupported new vendors instead of generic login. | Official apps/terminals, browser/device codes and expired-account UAT. | `official-auth-matrix` / managed-auth owner. | Parser/session tests for each closed state; one real login, expiry and logout readback per supported provider. | No. |
| #47 preserve existing config and explicit replace | Shared config/change-plan architecture has current target snapshots, overwrite tokens and recovery (`src/pages/models/Page.tsx:401-549`; `src/shared/features/change-plans.ts:266-317`; `tests/renderer/pages/models/apply/ChangePlanWorkspace.test.tsx`). | Add an explicit keep-current vs replace decision at entry, actual live target readback after write, external-change conflict handling for all current config paths, and first-use handoff from #34. | Existing user configurations and external concurrent edits on each target. | `existing-config-choice` / configuration workspace owner. | Renderer/native tests for keep, replace, stale plan and external edit; one local target readback. | No. |
| #56 preview actual changes and recovery | Shared `ChangePlan` and `ApplyWorkspace` already expose resources/effects, confirmation and rollback/readback (`src/shared/features/change-plans.ts:29-152,570-632`; `tests/renderer/pages/models/apply/ApplyWorkspace.test.tsx:135-299`; `.trellis/spec/frontend/change-plan-workspaces.md:91-181`). | Audit current Codex/WorkBuddy paths so preview lists only actual changed files/fields and required restart/impact; ensure plan identity is rechecked immediately before apply and config drift forces re-preview. | Native restart/actual file effects and platform-specific config behavior. | `change-preview-gap-audit` / change-plan owner. | Existing tests extended with changed/unchanged resources, stale digest, restart-needed and rollback result; local save/readback. | Mostly, but actual runtime path still needs local evidence. |
| #61 failure recovery after config write | Atomic backup, external-change guard, restore receipt and UI recovery button exist (`src-tauri/src/config/recovery.rs:203-273,317-415`; `src/shared/features/controls/FileRecoveryButton.tsx:1-120`; `tests/renderer/features/FileRecoveryButton.test.tsx:47-101`). | Apply the same recovery contract to every currently used provider/config writer, including partial multi-target results and idempotent retry; identify writers still bypassing recovery (`provider/live.rs` is a likely audit target). | Failure injection against real paths and cross-process edits. | `writer-recovery-coverage` / native config writers + change-plan owner. | Static writer inventory plus failure-injection tests proving baseline restore, conflict preservation, and no duplicate damage. | No for complete closure. |
| #64 reliable takeover/exit | Proxy control has takeover/switch/recovery contracts (`.trellis/spec/backend/proxy-runtime.md`; `src-tauri/src/commands/sync_support.rs`, `src-tauri/src/services/provider/managed_proxy.rs`); config recovery primitives are available. | Add a user-facing takeover/exit plan that previews affected files/restart, snapshots before takeover, reads back proxy state and target config, and detects external modifications on exit. | Running external Agent processes, proxy ports and real restart/connection checks. | `proxy-takeover-recovery` / proxy runtime owner. | Native tests for plan/rollback/conflict plus local running-process takeover and exit readback. | No. |
| #67 release owners/certificate recovery | Release workflow has protected provider mode and machine evidence (`.github/workflows/release.yml:767-1128,1505-1645`), but no named human owner/contact or certificate expiry/rotation record is in the checkout. | Add a secret-free runbook/maintainer metadata under the approved release docs, with owner/contact, expiry/rotation procedure and failure recovery, linked from workflow. | Actual organization owners, certificate/account expiry dates, protected secret access. | `release-ownership-runbook` / release maintainer (docs/operations). | Review runbook against protected workflow; dry-run provider selection/absent-provider failure. | No; ownership and credentials are external. |
| #68 signed Windows x64/arm64 installers | CI builds separate x64/arm64, seals artifacts and generates signing/attestation evidence (`.github/workflows/release.yml:318-470,596-1128,1505-1645`); repository tests assert unsigned preflight boundaries. | No product code gap substitutes for signing: ensure formal workflow verifies publisher, cert SHA-256, timestamp, chain, artifact and embedded executable/uninstaller, and blocks publish on incomplete evidence. Add any missing verifier assertion only after examining actual signed artifact shape. | Signing certificate/provider, Windows clean VM, SmartScreen/UAC/install/upgrade/uninstall. | `windows-signing-physical-evidence` / release workflow owner. | Formal workflow run + `signtool`/PowerShell readback for both architectures and clean Windows UAT. | No. |
| #70 About/version/support | Version contract exists (`scripts/version.mjs`, `package.json`, `.trellis/spec/backend/fyagent-version-contract.md`); support links/docs exist in README/SUPPORT, but no obvious About route/component was found in `src/app` or shell pages. | Add shell About entry with product summary, runtime version, support/update links and on-demand release metadata; source version/link values from existing contract and avoid stale literals. | Link/account validity and release metadata publication. | `about-support-surface` / shell/frontend owner. | Renderer route test asserts current package version and labelled links; static version contract check. | Yes for UI/code; link freshness remains operational. |
| #73 export/import selected config | Deep-link import is security-bounded (`src/domain/configuration/serialization/deepLinkConfigPreview.ts`; `src-tauri/src/deeplink/*`); provider serialization and redaction tests exist (`tests/domain/serialization/providerConfigStructural.test.ts`, `providerConfigUtils.codex.test.ts`). | Add versioned selected-config export/import schema with size/path/field allowlists, dry preview (overwrite/skip/rename/credentials needed), confirmation-bound save and recovery/readback; route deep links through the same preview. | Cross-device compatibility, credential re-entry and user-selected files. | `workspace-pack-import` / serialization + change-plan owner. | Schema/parser fixtures with secrets/paths/unknown fields; import preview then save/readback and failed-write recovery. | Mostly code, but cross-device acceptance is not. |
| #92 usage demo assets | Browser tests produce screenshots for first-use flows (`tests/browser/first-use-guide.spec.ts:46-65,131-150`); docs contain shot-card references (`docs/user-manual/assets/shot-cards/004-about-tool-install.md`). | Produce a small current-flow screenshot/video set with captions, alt text, source/version manifest and publish-operation mapping; validate every frame against actual 0.4.6 behavior. | Human-recorded video, subtitle/alt-text editing, source files and publication account/credentials. | `demo-assets-0.4.6` / docs/content owner. | Review asset manifest and replay each demonstrated flow on a clean local candidate. | No. |

### Work-package ordering

1. `provider-secretref-migration` (#35) is the dependency for the provider-sensitive portions of #40, #42, #47 and #73; #35 has a separate implementation owner.
2. `change-preview-gap-audit` + `writer-recovery-coverage` (#56/#61/#47) should share the existing ChangePlan/recovery contracts, followed by `multi-target-provider-apply` (#42).
3. `first-use-direct-entry` (#34) depends on the catalog/configuration route and should be done alongside `existing-config-choice` (#47).
4. `install-preflight-summary` + `install-identity-recovery` (#27/#29) are one install lifecycle slice; their final evidence is OS-specific.
5. `provider-preset-matrix` (#40) and `official-auth-matrix` (#43) need current vendor facts/credentials and should not be closed from fixtures alone.
6. `about-support-surface` (#70) and `catalog-provenance-ui` (#25) are comparatively isolated code/doc slices.
7. #67, #68 and #92 are operations/evidence packages. They must not be represented as already complete because workflow code or fixture screenshots exist.

## Files found

- `.trellis/tasks/09-20-next-iteration-engineering/research/issues-initial.json` — authoritative merged issue bodies and acceptance conditions.
- `.trellis/tasks/09-20-next-iteration-engineering/prd.md` — parent task constraints and current iteration scope.
- `.trellis/spec/frontend/index.md`, `.trellis/spec/backend/index.md` — focused contract indexes.
- `.trellis/spec/frontend/agent-directory.md`, `agent-auth.md`, `change-plan-workspaces.md`, `.trellis/spec/backend/external-agent-lifecycle.md`, `external-agent-sources.md`, `secretref-backend.md`, `proxy-runtime.md`, `github-release-workflow.md`, `fyagent-version-contract.md` — relevant boundaries and test expectations.
- `src/pages/agents/**`, `src/shared/features/{agents.ts,agent-install-readiness.ts,change-plans.ts,managed-auth.ts}` — renderer contracts/catalog/lifecycle/change plans.
- `src-tauri/src/agent_install/**`, `src-tauri/src/config/recovery.rs`, `src-tauri/src/services/{provider,secret,managed_auth}/**`, `src-tauri/src/deeplink/**` — native installers, recovery, provider/secret, auth and import boundaries.
- `tests/browser/first-use-guide.spec.ts`, `tests/browser/agent-directory.spec.ts`, `tests/renderer/pages/models/apply/**`, `tests/renderer/features/{FileRecoveryButton,change-plans,managed-auth}.test.*`, `tests/codexUserHelperContract.test.ts` — existing evidence and remaining test seams.

## External references

No external web research was performed. Vendor URLs and certificate facts are intentionally treated as unknown/currentness-sensitive inputs; the repository's allow-list/source tests are the only external-source evidence used here.

## Related specs

See the focused contracts listed above. The strongest cross-cutting rule is the authority order in `.trellis/spec/README.md`: current source/tests outrank specs and historical task notes. This report therefore does not mark an issue closed merely because a PR, fixture, workflow, or static test exists.

## Caveats / Not Found

- `src/pages/app`/shell search found no obvious About route/component; this is a targeted search, not proof that no equivalent menu exists.
- #35 was intentionally limited to interface/dependency location as requested; no migration design is asserted.
- #67 owner/contact, #68 signing/clean-Windows evidence, and #92 demo assets are absent from the checkout and cannot be inferred from code.
- A green fixture/static test is not service readback, UAT, release, or production evidence.

### Boundary corrections (2026-09-20 follow-up)

- #92 does not require human recording as a prerequisite. The issue permits a reproducible recording made from current fixtures, provided captions, alt text, source/version manifest and demonstrated behavior are checked. The gap is therefore an executable content-production package, not an inherently external-only blocker.
- #42/#47/#56/#61/#64 are not automatically “non-code close” merely because they require readback. Temporary-directory native saves, failure injection, conflict simulation and service readback can be implemented and verified by the engineering run. Mark them incomplete until that evidence exists, but do not classify the evidence as impossible for code execution.
- #67 and #68 still require external ownership/certificates or physical Windows signing/UAT; #25/#40 require current official source facts and (for connectivity) suitable credentials, while no secrets are needed for the source research itself.
