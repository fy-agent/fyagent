# Execution

1. Capture branch/worktree baseline and exact published CI failures.
2. Reproduce the two inventory failures and workstation-path privacy failure.
3. Repair their existing owners; review the retained health screenshot.
4. Run focused contracts, complete local gate, then production/browser tests
   serially. Keep prior performance and native readback bounds explicit.
5. Validate task context, run prearchive, commit the narrow work, archive this
   task and validate final tracked documentation and repository contracts.
6. Recheck main drift and merge-tree, fast-forward the existing PR branch,
   read back hosted checks and preserve the original shared worktrees.
7. Finalize the local coordination packet; never claim a session was created
   without a real new thread identifier/readback.
