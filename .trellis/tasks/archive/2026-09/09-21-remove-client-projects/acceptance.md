# 最终验收 — 2026-09-22

状态：本机代码验收通过。主体由 Grok 实施，GPT 复核并完成收尾。基线为 `c0b2ec21caccd33c082884270aa2e5dbfb7e9d9e`。

客户项目、交付包、验证及交接的界面和专属前后端功能已移除。旧路由回退到 AI 软件配置。共享配置和真实文件保留，新安装不创建退役存储。

复核发现的触发器误放行与同步丢失历史数据已修复并添加回归。旧数据库升级保留历史行；后续替换数据库前先写入并核验 `retired-customer-projects/historical-fde-*.db` 持久快照，失败则停止替换。快照是本地完整数据库备份，不参与同步或普通备份轮转。恢复只接受封闭历史触发器定义，在候选库维护写入前禁用并删除；中文名称校验不会触发 UTF-8 切片崩溃。

最终 `mise run check` 退出码为 0，涵盖：

- 类型、lint、格式、编译、Clippy、环境和仓库契约检查通过。
- 单元测试：206 文件、2020 通过、1 跳过；桌面模拟测试 7 项通过。
- Rust：19 套测试、3862 通过、6 忽略、0 失败。
- release:check 及其附属契约测试通过；附属测试 652 通过、1 跳过，native-fetch 模拟测试 4 项通过。
- `mise run test:browser`：651 项 Chromium/WebKit 回归以及 3 项生产构建启动检查通过。后续仅改变后端与文档，浏览器证据继续有效。
- 三语手册清理后 121 个相对链接均存在；`git diff --check` 通过。

之前模型页在并发负载下失败的 5 项，单独复验和最终完整检查都已通过，没有修改共享业务或放宽超时。

本机测试使用隔离数据库及受控接口。未做 Windows 实机、打包 Tauri 应用、真实账号或生产数据运行验收。Session 替换记忆仅完成独立调研，不在本变更中实现。

原规划的“仅规划、不实施”描述是初始阶段记录，已被后续实施授权及本验收取代。执行历史和返工详情见 [execution-result.md](./execution-result.md)。创建 PR 不代表发布或授权自动合并。
