# FyAgent 控制面 v4 生成提示词

## 共同母版

所有最终页面继承 `02-models-v4.png`：16:10 跨平台桌面控制面、深蓝灰毛玻璃、三栏结构、六个一级胶囊导航、Windows 右上窗口控件、warm off-white 高可读文字、每页最多一个实心 signal-blue 主动作。禁止纯黑、白玻璃、紫色渐变、霓虹泛光、嵌套发光卡、正文小于 14px、伪指标和伪运行状态。

## 01 Agent 目录

> 左栏使用 Qwen Code / WorkBuddy / QClaw / Codex / Claude Code 受控目录，并同时显示 CAP-100 目录身份与运行时状态。Qwen Code 选中，详情显示 P0、CLI Agent、待适配、规划能力和当前尚无适配器；右栏动作是“查看接入要求”。P0 不得表达成已安装或已连接。

## 02 模型

> 当前应用为 Codex，页内视图为接入源 / 路由 / 用量，接入源选中。左栏显示 OpenAI 官方、OpenRouter、ModelScope、自定义接入及认证/状态；中栏显示分类、认证、API 格式、模型、Base URL、备注；右栏显示当前使用、代理接管、故障转移、用量查询和测试/编辑/复制。不要五 Agent 全局路由。

## 03 Skills

> 已安装 / 发现 / 来源 / 备份四个页内视图。左栏列表显示名称、来源、启用应用数和更新状态；中栏显示描述、目录、来源、仓库、分支、更新时间和检查状态；右栏只显示 Claude、Codex、Gemini、Grok Build、OpenCode、Hermes。删除能力胶囊和候选 Agent 开关。

## 04 MCP

> 服务 / 分配两个页内视图。左栏显示名称、stdio/HTTP 与启用应用数；中栏显示 ID、名称、描述、传输方式、命令、参数、工作目录、环境变量数量、标签和文档；右栏只显示真实支持应用。删除连接健康、工具数量和权限识别。

## 05 提示词

> 当前应用为 Codex。左栏显示提示词启用开关、名称、描述和状态；中栏显示名称、描述和 Markdown 内容；右栏显示 Codex、目标文件 AGENTS.md、启用状态和“一次只启用一条”。删除共享模板、优先级、版本历史和跨 Agent 分配。

## 06 记忆

> 页内视图为 OpenClaw / 每日记录 / Hermes，OpenClaw 选中。左栏列出九个真实工作区 Markdown 文件；中栏编辑 MEMORY.md；右栏显示归属、类型、格式、存在状态和本机存储。删除跨 Agent 可见范围、向量、自动抽取和云同步。
