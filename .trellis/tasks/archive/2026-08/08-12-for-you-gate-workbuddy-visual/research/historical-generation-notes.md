# v1 / v2 历史生成依据

> 说明：v1、v2 当时没有保存逐字的完整工具调用。本文件依据当轮任务记录、评审结论和已保存图像重建生成约束，不能声称是原始逐字提示词。v3、v4 的提示词档案见各自 `generation-prompts.md`。

## v1｜WorkBuddy For You Gate

参考输入：

- `workbuddy-baseline-1440.png`：当时的 WorkBuddy 运行结构。
- `fyagent-brand-board.png`：graphite / blue / cyan token。
- `fyagent-app-icon-concept.png`：Y 形入口符号。
- `05-agent-constellation-hero.png`：生态连接构图。

重建提示约束：

> 生成一张 WorkBuddy 高保真桌面配置原型。以深色 graphite 为底，保留 FyAgent Y 形入口符号，把状态、连接配置、模型选择和保存组织成连续三栏任务路径。右侧可使用低对比 hub-and-spoke 连接图，但不得暗示真实硬件。只显示“尚未连接”等中性状态，不伪造模型列表、成功状态、指标或日志；不替换品牌主文件，不加入机器人、锁、钥匙和盾牌等通用隐喻。

## v2｜浅色完整控制面

参考输入：

- v1 图像。
- For You Gate Y 形符号。
- CAP-100 受控目录结论。

重建提示约束：

> 把 v1 编辑为浅雾蓝、半透明毛玻璃和胶囊按钮的 FyAgent 控制面。首屏从 WorkBuddy 单页改为 Qwen Code、WorkBuddy、QClaw、Codex、Claude Code 五个受控候选；Qwen Code 标记 P0。顶部新增 Agent 目录、模型、Skills、MCP、提示词、记忆六个一级入口，删除 workspace/星座主视觉。候选只写来源、权限、接入路径等中性说明，不写成已安装、已验证或已成功。

## 已知替代关系

- v1 的 WorkBuddy 单页和星座主视觉被 v2 替代。
- v2 的卡片墙、说明性小字、红黄绿窗口控件被 v3 替代。
- v3 的浅雾白玻璃和超出现有数据契约的能力表达被 v4 替代。
