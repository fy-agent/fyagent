# WorkBuddy 高保真原型 v1

## 产物

- 图像：`workbuddy-for-you-gate-prototype-v1.png`
- SHA-256：生成后由本地 `Get-FileHash` 复核。
- 生成方式：Codex 内置 `image_gen`。
- 用途：视觉评审，不是产品运行截图，也不代表已经实现。

## 参考图角色

1. `workbuddy-baseline-1440.png`：当前主线运行时结构基线。
2. `fyagent-brand-board.png`：R4 选定的 graphite / blue / cyan 设计语言。
3. `fyagent-app-icon-concept.png`：For You Gate 的 Y 形入口符号真源。
4. `05-agent-constellation-hero.png`：生态连接构图参考；原型仅转译 hub-and-spoke 关系，不表达真实硬件。

## 生成约束

- 保留 WorkBuddy 的状态、连接配置、模型选择、保存四类真实语义。
- 中性状态为“尚未连接”，不伪造成功、模型列表、指标或运行日志。
- 不替换 `assets/fyagent.png`，不引入机器人、锁、钥匙、盾牌等通用隐喻。
- 不使用紫色渐变、玻璃拟态、大圆角卡片墙或无功能装饰。

## 评审关注点

- 三栏“进度—连接—模型”的节奏是否比当前长表单更清楚。
- Hero 的品牌表达是否过强，是否需要进一步让位于操作界面。
- 05 的连接图是否足够克制、没有形成硬件或已实现能力暗示。
- 深色 graphite 方向是否接受，还是需要保留当前浅色系统做同构版本。

## 方向说明

本轮没有额外生成 02/04/07/08 方向变体，因为飞书 R4 已明确选定 01 / For You Gate 和 05 作为本周软件视觉；重新发散会违反已确认范围。若 v1 评审要求对比，只在同一方向内做布局/明暗度变体。
