# 首屏就绪与窗口显示体验

正常启动不展示仅加载中的路由空壳；首屏路由优先，其余路由后续预取。就绪不依赖无限等待网络。保留静默启动、显式激活、关闭/退出、恢复和初始化错误；加载失败可见且可恢复，不能永远隐藏。

验收：慢 chunk 下首屏 ready 不早发、初始页面完成后只发一次；失败路径可见；后台预取不造成未处理拒绝；native show 与既有隐藏/激活策略兼容。自动化生命周期、原生可移植测试、类型/构建/浏览器与完整门禁通过。性能证据只报告实际测量或结构变化，不编造毫秒收益。
