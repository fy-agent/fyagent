# CLI space decision

The user explicitly added Jev participation on 2026-09-21. Root supplied the
current Issue criteria, existing exact npm manifest/helper architecture,
observed package dependency shapes, size-evidence limits, and four alternatives
to `jev_decide`. The first request was rejected before execution because the
tool accepts at most three requirements; the corrected request ran once.

Observed service: `typesafe-ai/jev`, provider `vercel`. It selected the bounded
metadata approach. Raw output is retained in `jev-cli-decision.json`.

GPT-6 decision: proceed with the existing exact-version/platform/integrity
manifest and a clearly labelled product reserve; freeze the package resolution
and check actual destination/cache/temp volumes at confirmation and execution.
Keep unknown native-updater capacity explicit with the official handoff. Bump
the helper protocol when adding budget fields so old helpers cannot ignore them.
Do not introduce a general installer or treat the desktop cap as CLI evidence.

Jev's probabilities are advisory semantic judgments over supplied evidence.
They do not prove implemented version binding, dependency completeness, disk
admission, helper compatibility, native execution or Issue completion. Those
remain pending code review and tests in the isolated CLI follow-up.
