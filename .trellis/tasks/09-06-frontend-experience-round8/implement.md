# Execution and acceptance

- [x] Read clean current baseline, workflow, shared layout/assignment/reuse
      contracts and installed panel API; inspect the provided screenshot.
- [x] Reproduce A→B→A assignment inflation in WebKit and inspect Chromium
      control; compare bulk wrapping, actual pane widths and card widths.
- [x] Run isolated CSS candidates and a real-divider density study against
      unchanged production assets; record limitations and rejected options.
- [x] Present final plan and obtain a subsequent explicit approval before
      task.py start or product implementation. No approval is inferred from
      the initial request to create this task.
- [x] Recheck HEAD/status/worktree registry; start this task through the
      current Trellis context and read before-dev plus changed-layer specs.
      Use one owner or isolated worktree, never concurrent writes to shared CSS.
- [x] Add a failing browser regression for Skills and MCP: seven original
      target labels, 1564→1232→1180→900→1181→1564 sequence, natural row-height
      bounds and atomic bulk action pairs; include both engines/themes.
- [x] Apply minimal intrinsic-row correction; extract the existing duplicated
      bulk section to one presentation owner and container-based row template.
      Preserve disabled/native callbacks and explicit modal origins.
- [x] Extend SplitPanes sizing policy for middle-flexible three-pane detail;
      share defaults/min/max, remove inactive Skills/MCP sizing declarations,
      retain two-pane custom policies, library handles and static fallback.
- [x] Implement shared info-card and metadata density roles using actual
      container size; audit Auth/Models/Prompts/Memory consumers, copy-only
      paths, long labels/URLs and natural-height short cards.
- [x] Validate real divider drag, keyboard resize/reset, continuous window
      changes and both sides of 1181px plus internal card/action thresholds.
      Four desktop sizes, 320px detail stress, 616px equivalent zoom pressure,
      font enlargement, hidden-route return and dialog close must preserve
      controls, scroll offsets where appropriate and the same MCP draft node.
- [x] Review before/after screenshots with local-import and repository Skills,
      populated/empty/error states; make final values explicit in shared owners.
      Do not weaken assertions or add fixed row heights/repaint timers.
- [x] Run `mise run typecheck`, `mise run lint`, `mise run format:check`,
      `mise run test:unit`, `mise run test:browser`, `mise run build:renderer`.
      Run `mise run test:performance` serially with no concurrent compiler or
      functional-browser jobs; keep prior budgets and report stress costs.
- [x] Update actual SPEC signatures, role boundaries and negative cases;
      align assignment docs with existing API, not a speculative variant.
      Revalidate all task contexts and changed SPEC relative links.
- [ ] Run full `mise run check:prearchive --exclude-active-task
  .trellis/tasks/09-06-frontend-experience-round8`; commit only this task's
      verified implementation/SPEC, set work_commit and archive.
- [ ] Merge any owned worktree, confirm all work commits are ancestors, clean
      safely without force, repair relocated task contexts, run no-exclusion
      `mise run check:contracts`, record Journal and verify clean Git state,
      no active task and no owned worktree/branch residue. No push or release.
