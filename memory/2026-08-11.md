# 2026-08-11

- FyAgent 文档重构计划完成：根 README 保留中英日三语，用户手册统一为六章 / 每语
  28 篇，60 个旧章节以 `R100` 提交保留历史，旧品牌截图退出 README。
- 手册 40 张 PNG / 84 处引用已完成逐图审计，建立 15 张拍摄任务卡；真实 UI 与营销
  概念图继续分层。v2 因左右线路、端口和弯折规则不一致改为 `superseded`，统一六入六出线路网格的 v3 成为当前 `concept_candidate`，仍不得直接发布。
- 用户确认 FyAgent 是 VibeKey 更名后的产品延续，名称解释为 `For You Agent`，对外理念是“AI 时代的个人随身数字人格”。README 同时说清产品愿景来自 VibeKey、硬件载体转为跨平台软件，以及当前代码从 CC Switch 演进的事实。
- `mise run check:contracts` 和 Windows x64 Developer PowerShell 下的完整
  `mise run check` 均通过；后者耗时 528.2 秒，覆盖 TypeScript、Vitest、合同与 Rust
  check/clippy/test。
- Windows 收口时修正三个可复用测试边界：`.mjs` 固定 LF，macOS shell 合同测试从 Git
  安装定位 Bash 并读取 Git mode，文档扫描排除并发格式测试的临时目录。
